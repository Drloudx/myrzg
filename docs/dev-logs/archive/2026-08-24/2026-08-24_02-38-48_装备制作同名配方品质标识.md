# 兑换页装备条目模块归属核查
> 生成时间：2026-08-24T02:38:48+08:00
> 触发来源：确认兑换页中的 `baodimap*` 条目是否属于装备制作模块

## 修改范围
- `src/utils/exchangeData.js`
- `src/views/ExchangeView.vue`
- `public/data/parsed/parsed-exchange.json`（由数据构建重新生成）
- `docs/SPEC.md`
- `docs/ARCHITECTURE.md`

## 变更详情
- 对照 `exchangeTeam.json`、`ProducePanel.cs`、`SmithPanelUI.cs` 和 `ItemExchangePanelUI.cs`，确认正式锻造入口是 `ProducePanel → SmithPanelUI`，读取独立的 `itemExchangeRandom.json` 与 `blacksmith.randomTeamList`。
- `itemExchange.json` 中的 `baodimap*` 普通记录仅有 `category[0] = 装备兑换`，且没有被 `exchangeTeam.json` 的普通 `teamList` 引用；它们属于兑换配置表中的固定品质/保底类记录，不能当作锻造台模型解释。
- 兑换页保持 `itemExchange.json` 数据流，栏目命名修正为“装备兑换”；不把随机锻造数据搬入兑换页。

## 验证与测试
- `npm run data:build`
- `npm run verify`
- `npm run data:build` 后确认兑换页仍来自 `parsed/parsed-exchange.json`，未引入 `itemExchangeRandom` 产物字段。

## 遗留问题 / 未完成点
无

## 发布影响提示
需要随前端代码和 `parsed-exchange.json` 一起发布；不影响旧数据回退。

## 文档同步动作
- ✅ `docs/SPEC.md` 兑换页模型与卡片规则
- ✅ `docs/ARCHITECTURE.md` 数据流说明
- ✅ `docs/dev-logs/README.md` 索引
