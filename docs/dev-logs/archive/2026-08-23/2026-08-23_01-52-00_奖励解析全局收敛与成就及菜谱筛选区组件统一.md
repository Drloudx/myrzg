# 2026-08-23 01:52:00 奖励解析全局收敛与成就及菜谱筛选区组件统一

## 1. 需求与背景
- 用户反馈与需求：
  1. **奖励解析 DRY 收敛**：检查并清理「成就查询」等页面对银币、氪金等基础货币的重复手动处理，统一收敛至全局 `src/utils/gameMappings.js` 统一解析。
  2. **筛选区组件统一**：将「成就查询」与「菜谱查询」顶部中间区域的筛选组件替换为与「物品图鉴」相同的 `UiSearchInput` + `UiFilterRow` + `UiFilterPill` 样式，并在筛选行右侧保留原有的数量/收集统计。
  3. **收集开关颜色微调**：参考用户红线示意图，将成就卡片中收集滑块的激活状态颜色由湖青色改为与顶部页签一致的深原木色（`var(--wood, #2b1f15)`）。

## 2. 排查与技术方案
1. **全局奖励解析收敛（`gameMappings.js`）**：
   - 在 `src/utils/gameMappings.js` 中封装并导出 `parseRewardObject(rewardObj, itemMap)` 函数。
   - 自动遍历 `BASE_REWARD_ICONS`（涵盖银币 `money`、氪金 `ke`、神晶 `payKe`、经验 `exp` 等全部 8 类基础货币），并解析 items 道具规则与对应中文名。
   - 彻底删除 `AchievementView.vue` 内的手动货币分支，全量接入 `parseRewardObject`。
2. **`UiFilterRow` 组件扩展**：
   - 为 `UiFilterRow.vue` 增加 `#right` 插槽与 `.ui-filter-row__right` 布局样式，支持在筛选胶囊行最右侧放置统计信息或操作按钮。
3. **成就与菜谱筛选栏统一**：
   - `AchievementView.vue`：引入 `UiFilterRow` 与 `UiFilterPill` 呈现分类行与状态行，状态行右侧挂载 `已收集 X / Y` 计数器。
   - `RecipesView.vue`：引入 `UiFilterRow` 与 `UiFilterPill` 呈现标签行，右侧挂载 `数量：X / Y` 计数器。
4. **收集开关配色调整**：
   - 将 `.ach-switch-toggle.is-active .switch-track` 的背景色设为 `var(--wood, #2b1f15)`（暗色模式为 `var(--wood-soft, #463424)`），边框为 `#17100a`，与羊皮纸原木色系完美契合。

## 3. 变更详情
- `[MODIFY]` [src/utils/gameMappings.js](file:///e:/Desktop/html/myrzg/vue-myrzg/src/utils/gameMappings.js):
  - 导出 `parseRewardObject(rewardObj, itemMap)` 通用解析函数。
- `[MODIFY]` [src/components/ui/UiFilterRow.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/components/ui/UiFilterRow.vue):
  - 新增 `#right` 插槽支持右侧统计与拓展内容。
- `[MODIFY]` [src/views/AchievementView.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/views/AchievementView.vue):
  - 接入 `parseRewardObject` 数据解析。
  - 顶部筛选栏替换为 `UiFilterRow` + `UiFilterPill` 结构，保留右侧收集计数。
  - 收集滑块激活颜色改为深原木色。
- `[MODIFY]` [src/views/RecipesView.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/views/RecipesView.vue):
  - 顶部筛选栏替换为 `UiFilterRow` + `UiFilterPill` 结构，右侧保留食谱数量统计。

## 4. 验证与测试
- 运行 `npm.cmd run build` 预处理与构建编译通过（165 模块，0 报错）。

## 5. 后续事项与注意事项
- 全站图鉴与查询界面的顶部筛选交互风格已达到高度一致性，奖励数据源严格遵循 DRY 规范。
