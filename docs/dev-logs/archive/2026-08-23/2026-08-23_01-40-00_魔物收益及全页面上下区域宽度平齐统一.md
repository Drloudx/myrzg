# 2026-08-23 01:40:00 魔物收益及全页面上下区域宽度平齐统一

## 1. 需求与背景
- 用户反馈：
  - 「魔物收益」页面的中间区域下半部分（表格列表区域）宽度比上半部分（筛选面板）短了一截。
  - 要求检查并确保所有页面中间区域的「上半部分筛选区」和「下半部分内容/网格/列表区」宽度严格左右齐平、100% 统一。

## 2. 排查与技术方案
1. **排查宽度缩进原因**：
   - 在 `PetsEggsView.vue` 中，下部表格滚动容器 `.pet-table-scroll` 存在 `padding: 4px 12px 12px` 的左右 12px 内边距，导致下方的魔物蛋数据表格整体被内缩了 24px，无法与上方全宽的 `.pet-filter-sticky` 左右平齐。
   - 在 `RewardsView.vue` 中，`.rewards-content` 存在 `padding: 4px 12px 12px` 且内部 `.reward-list-container` 限制了 `max-width: 900px`。
2. **全量修正与对齐方案**：
   - 修正 `PetsEggsView.vue` 的 `.pet-table-scroll` 内边距为 `padding: 2px 0 14px 0`，使表格宽度 100% 撑满中间列，与上方筛选面板两侧完全对齐。
   - 修正 `RewardsView.vue` 的 `.rewards-content` 为 `padding: 2px 0 14px 0`，并移除 `max-width: 900px` 限制，设为 `width: 100%; max-width: 100%;`。
   - 确认其他基于 `UiCardGrid` 的视图（`ItemsView`, `EquipsView`, `HeroesView`, `PetsView`, `RecipesView`, `MonstersView`, `AchievementView`, `TasksView`, `EventsView`, `ExchangeView`）其上下宽度均已保持 100% 齐平。

## 3. 变更详情
- `[MODIFY]` [src/views/PetsEggsView.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/views/PetsEggsView.vue):
  - 将 `.pet-table-scroll` 的 padding 调整为 `2px 0 14px 0`，消除左右缩进。
- `[MODIFY]` [src/views/RewardsView.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/views/RewardsView.vue):
  - 将 `.rewards-content` 的 padding 调整为 `2px 0 14px 0`，`.reward-list-container` 设为 `width: 100%; max-width: 100%;`。

## 4. 验证与测试
- 运行 `npm.cmd run build` 预处理与生产编译通过（165 模块，0 报错）。

## 5. 后续事项与注意事项
- 所有 12 个视图中间区域的上半部分（检索/筛选）与下半部分（网格/表格/列表）均已实现 100% 宽度左右平齐。
