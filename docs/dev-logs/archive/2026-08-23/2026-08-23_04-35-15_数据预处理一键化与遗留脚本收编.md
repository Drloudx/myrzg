# [2026-08-23 04:35:15] 数据预处理一键化：遗留脚本收编进统一发布链

## 1. 需求与背景
- 用户要求：检查 `scripts/` 下除 `scripts/parse/` 新脚本外其余脚本是否有存在必要；是否都能与新脚本**一键运行**，跑完即可直接发布版本，不需要单个单独运行；不合适的需重写/清理。

## 2. 评估结论
| 脚本 | 用途 | 结论 |
|---|---|---|
| `clean-data.js` | 生成 search-index.json / item-sources.json / data-types.d.ts，内部调用 parse-hidden-rewards | ✅ 有用，收编进统一入口 |
| `parse-rewards.js` | 生成 parsed-pvp.json（奖励页运行时）+ parsed-pvp-sources.json（clean-data 合并） | ✅ 有用，收编 |
| `parse-hidden-rewards.js` | 生成 parsed-hidden.json（奖励页运行时）+ parsed-hidden-sources.json | ✅ 有用，由 clean-data 内部调用 |
| `generate-affixes.js` | 生成 itemAffixes.json（装备词缀，itemParser 运行时） | ✅ 有用，收编 |
| `parse-exchange.js` | 生成 parsed-exchange.json（兑换页运行时） | ✅ 有用，收编 |
| `check-task-data.mjs` | 数据维护工具：从 Config_decrypted/GAoNano 同步裁剪 battle/room、复制 taskDialogs、生成 dialogIndex/dialogSegments 与引用校验报告 | ⚠️ 保留为**开发机数据维护工具**（依赖外部解密目录，不属于发布链；其产物已提交在仓库，发布无需重跑） |
| `fix-diary.cjs` | 一次性修复 diary.json 换行/尾逗号 | ❌ 一次性使命完成，已删除 |

## 3. 变更详情
- `[MODIFY]` [scripts/parse/index.mjs](file:///e:/Desktop/html/myrzg/vue-myrzg/scripts/parse/index.mjs): 升级为「一键数据准备」入口，顺序执行：
  1. `parse-rewards.js`（先产出 pvp 来源表）
  2. `clean-data.js`（内部调用 parse-hidden-rewards，合并来源、生成搜索索引/类型）
  3. `generate-affixes.js`
  4. `parse-exchange.js`
  5. 页面级预解析 9 域（items→tasks→recipes→achievements→events→pet-eggs→pets→heroes→monsters）
- `[MODIFY]` [package.json](file:///e:/Desktop/html/myrzg/vue-myrzg/package.json):
  - 新增 `data:build`（≡ `node scripts/parse/index.mjs`，仅数据预处理）
  - `build` = `npm run data:build && vite build`（**一条命令完成数据预处理 + 打包，直接发布**）
  - 保留 `data:clean` / `parse:data` 作为局部别名
- `[DELETE]` [scripts/fix-diary.cjs](file:///e:/Desktop/html/myrzg/vue-myrzg/scripts/fix-diary.cjs): 一次性数据修复脚本

## 4. 验证与测试
- `npm run data:build`：4 个遗留脚本全部成功（pvp 生成、search-index 1154 条、itemAffixes 89 条、兑换 13 分类 582 条）+ 9 个预解析产物全部生成
- `npm run build`（一键发布）：数据预处理 + vite build 全部通过（✓ built in 17.04s），`dist/data/parsed/` 含全部 17 个产物（9 个页面级 + 8 个既有表）

## 5. 后续事项与注意事项
- 发布流程：**`npm run build` 一条命令即可**；仅改数据不打包可用 `npm run data:build`。
- `check-task-data.mjs` 不建议进 release；若游戏版本更新导致任务/对话数据过期，由开发机手动执行该脚本刷新 battle/room/taskDialogs/dialogIndex 等原始/索引数据后再发布。
- 部署提醒：`dist/` 需随热更包/上传 CDN；代码对旧热更包自动回退原始多文件加载。
