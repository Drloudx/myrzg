# 2026-08-23 01:57:00 任务图鉴筛选区对齐与标签色调及标记开关柔和棕调优

## 1. 需求与背景
- 用户反馈：
  1. **奖励映射解释**：确认奖励解析完全复用 `BASE_REWARD_ICONS` 与 `BASE_REWARD_NAMES`，消除了视图层冗余硬编码。
  2. **成就标记开关棕色调优**：原本的深原木色（`#2b1f15`）在界面上视觉过于厚重，要求调整为一个柔和、温润但又有适度对比度的中度暖棕色。
  3. **任务图鉴筛选区组件对齐**：将「任务图鉴」顶部的分段页签改为与「物品图鉴」相同的 `UiSearchInput` + `UiFilterRow` + `UiFilterPill` 组件样式（主分类行 + 章节行 + 数量统计）。
  4. **任务标签色调统一**：将任务列表中任务标题旁的「主线」等主类型标签颜色改为与右侧「序章」标签一致的 `tone="wood"` 原木色。

## 2. 排查与技术方案
1. **成就滑块开关颜色微调**：
   - 将 `.ach-switch-toggle.is-active .switch-track` 调整为柔和的暖焦糖木棕色 `#745234`（暗色模式为 `#876241`），边框为 `#543922`。在羊皮纸底色上既保持了清晰的开关状态辨识度与白色 `✓` / 滑块对比，又具备温和优雅的质感。
2. **任务图鉴顶部重构（`TasksView.vue`）**：
   - 弃用 `UiSegmentedTabs`，引入 `UiFilterRow` 与 `UiFilterPill`。
   - 第一行：`UiSearchInput` 任务关键词/ID 搜索。
   - 第二行：`UiFilterRow label="分类："` 渲染主类型（主线、支线、委托等）。
   - 第三行：`UiFilterRow label="章节："` 渲染当前分类下的二级章节胶囊，右侧挂载 `数量：X / Y` 计数器。
3. **任务标签色系统一（`TasksView.vue`）**：
   - 将卡片与详情弹窗中的 `<UiTag :quality="Number(item.type)">` 统一替换为 `<UiTag tone="wood">{{ item.typeLabel }}</UiTag>`，使「主线」与「序章」具有完全一致的羊皮纸原木色调外观。

## 3. 变更详情
- `[MODIFY]` [src/views/AchievementView.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/views/AchievementView.vue):
  - 调整激活状态滑块为柔和暖木棕（`#745234` / 暗色 `#876241`）。
- `[MODIFY]` [src/views/TasksView.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/views/TasksView.vue):
  - 顶部筛选栏替换为 `UiFilterRow` + `UiFilterPill`。
  - 列表与弹窗中的主类型标签调整为 `tone="wood"`。

## 4. 验证与测试
- 运行 `npm.cmd run build`，165 个模块全部编译通过，0 错误。

## 5. 后续事项与注意事项
- 物品图鉴、成就查询、菜谱查询、任务图鉴的顶部检索与筛选已全面统一采用标准化 `UiFilterRow` 体系。
