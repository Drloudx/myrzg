# [2026-08-23 04:48:00] 外部 Markdown 整理与文档维护约定补全

## 1. 需求与背景
- 用户要求：排查 docs 之外的 md 是否过大/过期/冗余；执行清理；并询问「总规范是否写了操作后要更新哪些 md」。

## 2. 排查结论
- 根目录存在两份冗余/过期文档：
  - `UI_REFACTOR_GUIDE.md`（90 行）：旧版，仍引用已删除的 `common.css` 与「main.js 引入 common.css」；docs 版已为新版（theme.css），README 亦指向 docs 版 → 删根目录版。
  - `bak.md`（141 行）：过时的 json 文件名清单（早期“哪些文件待处理”记录）→ 删除。
- `SPEC.md`（442 行）仍有 5 处过期引用：`common.css`（×5，现为 theme.css）、`clean-data.js`（→ scripts/parse/search.mjs）、`parse-exchange.js`（→ scripts/parse/exchange.mjs）、`parse-hidden-rewards.js`（→ scripts/parse/hidden-rewards.mjs）。
- `docs/ARCHITECTURE.md` 第 11 行仍写 `scripts/clean-data.js`。
- 总规范（SPEC.md）此前**没有**「操作后要更新哪些 md」的约定（仅有 docs/dev-logs/README 的日志格式约定）→ 新增「十四、文档维护约定」。

## 3. 变更详情
- `[DELETE]` 根目录 `UI_REFACTOR_GUIDE.md`（旧版重复，保留 docs/UI_REFACTOR_GUIDE.md）
- `[DELETE]` 根目录 `bak.md`（过时文件清单）
- `[MODIFY]` [SPEC.md](file:///e:/Desktop/html/myrzg/vue-myrzg/SPEC.md):
  - 行 43/162/178/192/207：`common.css` → `theme.css`（含链接路径）
  - 行 138：`clean-data.js` → `scripts/parse/search.mjs`
  - 行 531：`scripts/parse-exchange.js` → `scripts/parse/exchange.mjs`
  - 行 544：`scripts/parse-hidden-rewards.js` → `scripts/parse/hidden-rewards.mjs`（映射表位置同步指向 src/utils/hiddenRewardsData.js）
  - 新增「十四、文档维护约定」：文档责任表（dev-logs / SPEC / ARCHITECTURE / UI_COMPONENT_LIBRARY / UI_REFACTOR_GUIDE 各自负责范围）+ 自检规则（grep 旧脚本/旧文件残留）
- `[MODIFY]` [docs/ARCHITECTURE.md](file:///e:/Desktop/html/myrzg/vue-myrzg/docs/ARCHITECTURE.md): 第 11 行数据来源更新为 `scripts/parse/index.mjs`（一键数据入口）

## 4. 验证与测试
- `grep` 全部 md：`common.css` / `clean-data.js` / `parse-*.js` / `generate-affixes.js` 仅剩历史 dev-logs 与新自检规则示例，无活动文档残留引用
- README.md / docs/UI_COMPONENT_LIBRARY.md / docs/UI_REFACTOR_GUIDE.md 经查为最新，无需改动

## 5. 后续事项与注意事项
- **暂未处理**（需用户确认后另做一轮）：SPEC.md 第四节调色板表格仍是旧「蓝色主题」数值（现为羊皮纸主题：--primary #557574 等）、`--header-height` 写 56px（实际 60px）、五节 `.page-view-container` 描述 max-width 800px（实际为 100% + 主区 grid 1400px）、`fetchWithFallback` 描述「自动加时间戳」（现仅 dev 生效）。建议下一轮把这些章节按当前 theme.css / App.vue 实际值重写。
