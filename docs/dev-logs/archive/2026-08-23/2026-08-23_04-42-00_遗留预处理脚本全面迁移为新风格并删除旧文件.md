# [2026-08-23 04:42:00] 遗留预处理脚本全面迁移为新风格并删除旧文件

## 1. 需求与背景
- 用户要求：不要保留旧风格脚本，全部改写成新风格（`scripts/parse/*.mjs` + `src/utils` 纯函数），数据链一键运行即可发布。

## 2. 方案
- 将 5 个旧预处理脚本的逻辑**等价迁移**为：
  - 纯函数构建器（`src/utils/*Data.js`，浏览器/Node 共用，无网络与浏览器依赖）
  - 新风格薄包装（`scripts/parse/*.mjs`，读原始 JSON → 调用纯函数 → 返回 {file, data}）
- `scripts/parse/index.mjs` 统一编排：pvp → 场景宝箱 → 搜索索引+物品来源+类型 → 装备词缀 → 兑换 → 页面级 9 域；写文件统一紧凑 JSON。
- 删除全部旧脚本；开发机维护工具 `check-task-data.mjs` 移至 `scripts/dev/`（需外部解密目录，不进发布链）。

## 3. 变更详情
### 新增纯函数构建器
- `[NEW]` [src/utils/pvpData.js](file:///e:/Desktop/html/myrzg/vue-myrzg/src/utils/pvpData.js): 原 parse-rewards.js 逻辑
- `[NEW]` [src/utils/hiddenRewardsData.js](file:///e:/Desktop/html/myrzg/vue-myrzg/src/utils/hiddenRewardsData.js): 原 parse-hidden-rewards.js 逻辑（含 HIDDEN_PREV_MAP）
- `[NEW]` [src/utils/searchData.js](file:///e:/Desktop/html/myrzg/vue-myrzg/src/utils/searchData.js): 原 clean-data.js 逻辑（搜索索引+物品来源+类型文件；不再生成 roles/equips）
- `[NEW]` [src/utils/affixData.js](file:///e:/Desktop/html/myrzg/vue-myrzg/src/utils/affixData.js): 原 generate-affixes.js 逻辑
- `[NEW]` [src/utils/exchangeData.js](file:///e:/Desktop/html/myrzg/vue-myrzg/src/utils/exchangeData.js): 原 parse-exchange.js 逻辑

### 新风格脚本（scripts/parse/）
- `[NEW]` scripts/parse/{pvp,hidden-rewards,search,affixes,exchange}.mjs：薄包装，返回 {files, deps?, typesContent?}
- `[MODIFY]` [scripts/parse/index.mjs](file:///e:/Desktop/html/myrzg/vue-myrzg/scripts/parse/index.mjs): 移除 execSync 旧脚本调用，全部改为模块编排（含 search 依赖 pvp/hidden 来源表的内存传递、类型文件写出）

### 删除/移动
- `[DELETE]` scripts/clean-data.js、scripts/parse-rewards.js、scripts/parse-exchange.js、scripts/generate-affixes.js、scripts/parse-hidden-rewards.js
- `[MOVE]` scripts/check-task-data.mjs → scripts/dev/check-task-data.mjs（开发维护工具，与发布链分离）

### 配置与文档
- `[MODIFY]` package.json: 仅保留 `dev` / `data:build` / `build` / `preview`；移除 data:clean、parse:data
- `[MODIFY]` SPEC.md / docs/ARCHITECTURE.md: 数据管线章节同步新脚本结构（scripts/parse/ + scripts/dev/）

## 4. 验证与测试
- `npm run data:build`：全部模块化步骤通过
- **内容级等价比对**（与迁移前 dist 产物对比）：search-index（980 条）、item-sources（212 键）、parsed-exchange（13 分类/582 条）、itemAffixes（89 件）、parsed-hidden（57 点位）、parsed-pvp **全部 JSON 完全一致**（体积变小仅因旧脚本美化输出 → 新管线紧凑输出）
- `npm run build`（一键发布）：数据预处理 + vite build 通过（✓ built in 17.44s）；grep 确认代码无对已删除脚本的引用

## 5. 后续事项与注意事项
- 发布：`npm run build` 一条命令 = 全部数据预处理 + 打包。
- 数据维护：`scripts/dev/check-task-data.mjs` 仅在游戏更新、需要同步解密源数据时由开发机手动执行（产物 dialogIndex/dialogSegments/裁剪表已提交在仓库）。
- 部署：dist/ 随热更包/上传 CDN；旧热更包自动回退原始多文件加载。
