# 2026-08-23 02:48:00 顶部栏三列对齐与全站图标色彩统一及装备图鉴统一

## 1. 需求与背景
- 用户需求：
  1. 调整顶部栏搜索区域，使搜索框与下侧中间主区域（`minmax(0, 1fr)`）同宽且左右起点严格对齐。
  2. 右侧图标（主题切换、设置齿轮等）色彩调整为与左侧页面大标题一致的柔光羊皮纸金（`#dfceb3`）。
  3. 页面标题下方的描述文本由「深渊之歌 · 手记」修改为「深渊之歌 · 资源库」。
  4. 顶部全局搜索框内部占位提示修改为「搜索名称、属性或描述...」。
  5. 装备图鉴（`EquipsView.vue`）排查并补齐与物品图鉴（`ItemsView.vue`）完全一致的 7 列网格布局与原生品质框/名称胶囊展示。
  6. 全面排查全站所有图标，将图标色彩统一调整为高质感的标准羊皮纸金/典雅棕。
  7. 按照开发规范，每次调整后均自动输出标准化 Markdown 开发日志。

## 2. 排查与设计实现
1. **顶部导航栏三列 Grid 精准对齐（`src/App.vue`）**：
   - 将 `.header-top-row` 改为 `display: grid; grid-template-columns: 250px minmax(0, 1fr) 300px; gap: 20px;`，与下方 `.main-layout-row` 严格一一对应。
   - `.desktop-search` 宽度设为 100%，居于第二列，与中间主内容区域在水平 X 轴上 100% 垂直严丝合缝对齐。
2. **全站图标色彩统一与滤镜校准**：
   - 顶部右上角图标 `.theme-icon-img`、`.setting-icon-img` 配置精确定制色彩滤镜 `brightness(0) saturate(100%) invert(88%) sepia(16%) saturate(380%) hue-rotate(345deg) brightness(96%) contrast(88%)`，呈现出与左侧标题完全一致的 `#dfceb3` 柔光羊皮纸金。
   - 顶部全局搜索放大镜 `.search-icon-img` 与清空按钮设为 `var(--paper, #dfceb3)`。
   - 筛选搜索框 `.ui-search__icon` 设为典雅棕 `var(--border-color, #8f7351)`，在浅色面板内对比鲜明。
3. **文案更新**：
   - `App.vue`：`.header-brand` 文案由「深渊之歌 · 手记」更新为「深渊之歌 · 资源库」。
   - `GlobalSearchBox.vue`：搜索框 placeholder 更新为「搜索名称、属性或描述...」。
4. **装备图鉴（`src/views/EquipsView.vue`）规范统一**：
   - 补齐 `.items-card-grid` 类名与 7 列响应式网格样式；
   - 装备卡片统一渲染游戏原版品质背景框（`item_f_1` ~ `item_f_6`）与对齐名称底框。

## 3. 变更详情
- `[MODIFY]` [src/App.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/App.vue):
  - `.header-top-row` 采用 3 列 Grid 布局与下侧三列对齐；
  - 标题下副文案修改为「深渊之歌 · 资源库」；
  - 右上角图标滤镜精确校准为 `#dfceb3`。
- `[MODIFY]` [src/components/GlobalSearchBox.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/components/GlobalSearchBox.vue):
  - 占位提示修改为「搜索名称、属性或描述...」；
  - 搜索与清空图标统一为 `#dfceb3`。
- `[MODIFY]` [src/components/ui/UiSearchInput.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/components/ui/UiSearchInput.vue):
  - 搜索图标色彩校准为 `#8f7351`。
- `[MODIFY]` [src/views/EquipsView.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/views/EquipsView.vue):
  - 补齐 7 列网格布局样式与 `.items-card-grid` 规范。

## 4. 验证与测试
- 浏览器热重载实时验证：
  - 顶部搜索框与下侧中间区域完全同宽并左右对齐；
  - 右上角图标呈现清澈的 `#dfceb3` 柔光羊皮纸金；
  - 搜索框文案与品牌副标题文案更新无误；
  - 装备图鉴呈现与物品图鉴完全一致的 7 列与品质框展示。
