# [2026-08-23 04:55:00] SPEC 移入 docs 与过期内容修正

## 1. 需求与背景
- 用户确认：将根目录 SPEC.md 移入 docs/（与其它规范文档统一），并同时修正此前发现的 4 处过期内容（旧蓝色主题调色板 / header 高度 / 页面容器宽度 / fetchWithFallback 时间戳描述）。

## 2. 方案
- 文档布局统一：根目录只留 README.md 入口，所有规范文档收进 docs/。
- 过期内容按当前 `src/assets/theme.css`、`App.vue`、`request.js`、`UiBackToTop.vue` 实际值修正（取自代码现状，非推测）。

## 3. 变更详情
- `[MOVE]` 根目录 `SPEC.md` → [docs/SPEC.md](file:///e:/Desktop/html/myrzg/vue-myrzg/docs/SPEC.md)（SPEC 内部链接均为 file:/// 绝对路径，移动不受影响）
- `[MODIFY]` [README.md](file:///e:/Desktop/html/myrzg/vue-myrzg/README.md): 文档列表新增 `docs/SPEC.md`（前端总规范）+ `docs/dev-logs/` 条目
- `[MODIFY]` [docs/SPEC.md](file:///e:/Desktop/html/myrzg/vue-myrzg/docs/SPEC.md):
  - 「十四、文档维护约定」自述 `SPEC.md` → `docs/SPEC.md`（2 处）
  - 第四节主题调色板表格：旧蓝色主题 → 当前羊皮纸主题实际值（--primary #557574 / --bg #bba282 / --bg-card #e9dcc3 / --text-main #3e2a14 / --border-color #8f7351 / --hover-bg rgba(85,117,116,0.14) / --modal-overlay rgba(24,14,6,0.55) 等，含暗色与别名）
  - `--header-height`: 56px → 60px
  - 五节页面容器：`.page-view-container`（100%/flex 列）/ `.max-w-wrapper`（860px）/ `.app-main`（100% + 三栏 grid 1400px + ≤1024 单列）按实际重写
  - 三节 request.js：`fetchWithFallback` 描述改为「dev 加时间戳；生产走 HTTP 缓存」
  - 五节悬浮按钮：`.nav-fab-btn` 44px / z-index 4000；`.back-to-top-btn`（已不存在的 BackToTop.vue）→ `UiBackToTop`（44px / z-index 900 / 滚动 100px 显示，含链接）

## 4. 验证与测试
- 文件移动：根目录 SPEC.md 不存在，docs/SPEC.md 存在
- grep `SPEC.md`：代码/配置无引用（仅 dev-logs 历史与 docs/SPEC 自述）；README 已指向 docs/SPEC.md
- 内容核对：调色板数值与 theme.css、header 高度 60px、页面容器与 App.vue、UiBackToTop 44px/z900 均一致

## 5. 后续事项与注意事项
- 文档布局最终形态：根目录仅 README.md；docs/ 含 SPEC / ARCHITECTURE / UI_COMPONENT_LIBRARY / UI_REFACTOR_GUIDE / dev-logs。
- 遗留说明：docs/SPEC.md「五节 UI 字典」中仍有少量历史类名条目（segmented-pill、category-tag-pill 蓝色样式、search-bar 等），当前页面已迁移到 `src/components/ui/` 组件体系；如需彻底对齐可再开一轮「按 UI_COMPONENT_LIBRARY 重写五节字典」。
