# [2026-08-23 05:08:00] UI 组件库文档页面骨架模板与项目实际对齐

## 1. 需求与背景
- 用户要求排查 docs/UI_COMPONENT_LIBRARY.md「4. 页面骨架模板（新页面照抄）」是否与实际项目一致；确认不一致后执行修正。

## 2. 排查结论（偏差）
1. 模板含 `<UiPageHeader>`：全项目任何视图都没有使用（页面标题由全局 App 顶栏提供）→ 移除并注明。
2. `UiCardGrid id="itemsGridScroll" small`：`small` 是真实 prop（88px 小图标卡），但当前无页面使用；物品/装备页用 `class` 自定义列数，任务页用 `wide` → 模板与物品页一致改为 `class="items-card-grid"`，并在注释说明 wide/small/class 三种用法。
3. 模板详情弹窗用 `fullscreen`：实际详情弹窗为内嵌模式 `max-width="820px" + scroll-id + :z-index`；`fullscreen` 仅特殊场景、`teleport-to="body"` 仅系统弹窗 → 重写。
4. 模板弹窗内容直接放 UiSection：实际均有 `<template v-if="selected">` 包裹 + 弹窗内挂 `UiBackToTop` → 补齐。
5. 「使用规则 3」同样写「全屏详情弹窗一律用 UiModal fullscreen」→ 按实际内嵌用法修正描述。

## 3. 变更详情
- `[MODIFY]` [docs/UI_COMPONENT_LIBRARY.md](file:///e:/Desktop/html/myrzg/vue-myrzg/docs/UI_COMPONENT_LIBRARY.md):
  - 使用规则 3：改为「详情弹窗用内嵌 UiModal（默认非 fullscreen），指定 scroll-id / max-width / :z-index；fullscreen 仅特殊场景；teleport-to="body" 仅系统弹窗」
  - 4. 页面骨架模板：移除 UiPageHeader；网格改 class 用法；弹窗改内嵌模式（max-width/scroll-id/z-index + template v-if + 弹窗内 UiBackToTop）；补充「页面标题由全局顶栏提供」说明与脚本导入示例

## 4. 验证与测试
- 比对真实页面（ItemsView/HeroesView）用法：模板结构、组件 props、弹窗参数与现状一致
- grep `UiPageHeader`：确认全项目无视图引用（组件仍保留在库内供将来可选使用）

## 5. 后续事项与注意事项
- UiPageHeader 组件保留在组件库（未删除），如未来确有页面内大标题需求可直接使用；按当前规范页面标题统一走全局顶栏。
