# 2026-08-23 02:39:00 物品卡片接入游戏原生品质框与名称胶囊视觉重构

## 1. 需求与背景
- 用户需求：
  1. 放弃纯 CSS 模拟的卡片背景，全面接入游戏原生物品背景框素材（`public/images/ItemBagPanel/item_f_1.png` ~ `item_f_6.png`），根据不同品质为物品卡片套上对应的游戏原版品质背景框。
  2. 调整框内图标比例，保持精致呼吸感。
  3. 为下方物品名称增加圆角背景底框（名称胶囊），扣除上方图片边框素材自带的 5px 透明留白，使名称底框与上方实际绘制的可见品质框左右边缘 1:1 严丝合缝垂直对齐。
  4. 调优名称胶囊背景色与文字色彩（采用 `#ceb896` 偏暖棕温润羊皮纸底色，搭配去除黑边描边的自然琥珀橙金 `#b05800` 等品质字色），确保字迹清晰易读且视觉自然协调。

## 2. 排查与设计实现
1. **接入游戏原生品质框（`UiItemCard.vue`）**：
   - 槽位 `.ui-item-card__slot` 动态绑定背景图：`background-image: url('/images/ItemBagPanel/item_f_${getQualityFrame(quality)}.png')`。
   - 支持品质 1（灰褐）、2（绿）、3（蓝）、4（紫）、5（橙金）、6（红）。
   - 图标尺寸收敛至 `max-width: 68%; max-height: 68%`，位于品质框正中。
2. **名称胶囊与边距扣除精准对齐**：
   - 上方 `item_f_*.png` 素材四周自带约 5px 的透明边距，因此将 `.ui-item-card__name` 设置为 `width: calc(100% - 10px); margin: 3px auto 0;`。
   - 彻底解决了底框比上方可见边框宽出的问题，实现像素级 1:1 对齐。
3. **字色与底色温润调优**：
   - 底色采用 `#ceb896` 偏暖棕的温润羊皮纸胶囊，配 `rgba(130, 98, 62, 0.55)` 细腻边框。
   - 品质 5（传说）字色调优为 `#b05800` 琥珀橙金，彻底去除生硬的黑影描边，呈现出自然高质感的手记视觉效果。

## 3. 变更详情
- `[MODIFY]` [src/components/ui/UiItemCard.vue](file:///e:/Desktop/html/myrzg/vue-myrzg/src/components/ui/UiItemCard.vue):
  - 动态渲染游戏原生品质框（`item_f_1` ~ `item_f_6`）；
  - 图标缩小为 68%；
  - 增加扣除 5px 留白的 `#ceb896` 名称胶囊与自然品质字色。

## 4. 验证与测试
- 页面热重载实时生效，各品质框与图标比例协调，名称胶囊与边框完美对齐，字色清晰自然。

## 5. 后续事项与注意事项
- 全站使用 `UiItemCard` 的图鉴网格均已统一采用游戏原生品质框与精准对齐的名称胶囊。
