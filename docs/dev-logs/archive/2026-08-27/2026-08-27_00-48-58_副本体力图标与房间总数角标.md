# 副本体力图标与房间总数角标
> 生成时间：2026-08-27T00:48:58+08:00
> 触发来源：副本进入消耗应复用全局体力图标；聚合节点角标应显示该位置的房间总数，而不是除当前房间外的额外候选数。

## 修改范围
- `src/views/DungeonsView.vue`
- `dist/`（由构建流程重新生成）

## 变更详情
- 进入消耗复用 `gameMappings.js` 的 `BASE_REWARD_PATHS.ti`，显示为体力图标与 `×数量`，不再显示“体力”文字。
- 聚合房间节点角标由额外候选数改为房间总数，例如 1 个当前房间加 6 个候选显示为 `7`。
- 保留数据层 `candidates` 的原始含义，仅在展示层加上当前房间，避免破坏展开和候选判断逻辑。

## 验证与测试
- `npm run verify` 通过（预解析产物、旧脚本残留检查、完整构建）。
- 构建产物确认包含全局体力图标路径、`×数量` 展示和聚合节点总数计算。

## 遗留问题 / 未完成点
- 无。

## 发布影响提示
- 页面模板与样式改变，需要重新构建并发布 `dist`。

## 文档同步动作
- ✅ 已追加开发日志并更新索引；本次未改变数据协议或全局 UI 规范，无需修改 `docs/SPEC.md`。
