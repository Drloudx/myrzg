# 副本箱子真实引用链与日志按日归档
> 生成时间：2026-08-27T04:08:00+08:00
> 触发来源：查明“前水洞 / 后刺蛇”的真实含义，修正副本特殊箱子来源，并整理开发日志归档规则。

## 修改范围
- `src/utils/dungeonData.js`
- `public/data/parsed/dungeons/*.json`
- `docs/dev-logs/README.md`
- `docs/dev-logs/archive/2026-08-27/`

## 变更详情
- 核对源码 `RewardConf`、`RoomCollectMsg` 与房间、采集、奖励配置，确认“前水洞 / 后刺蛇”是奖励 JSON 的编辑分类，不是路线名，也不参与客户端运行时反序列化。
- 特殊箱子汇总改为严格遵循房间实际引用的 `caijiTypeId → roomCollect → roomCollectType.reward` 链路，按 `rewardId` 去重。
- 移除按奖励分类反查并补齐同类箱子的逻辑，避免把未被任何房间引用的 `baoxiang323` 错挂到黑涧浴池。
- 特殊箱子恢复完整奖励池，不再只保留固定装备规则。
- 日志改为生成后立即按日期归档；根索引只保留日期入口，删除旧的逐条时间索引和 30 份阈值。

## 验证与测试
- `npm run build`：通过。
- `npm run verify`：全部通过，41 个副本详情文件均小于 2MB。
- 数据断言：`c3_d1_3_d` 实际箱子为 `313/312/311`；`c3_d2_3_d` 为 `313/322/321/311`；产物不含“前水洞 / 后刺蛇”，且未错误接入 `baoxiang323`。
- 最大副本详情文件约 0.73MB。

## 遗留问题 / 未完成点
- 原始配置存在不一致：`baoxiang323` 定义了黑涧浴池金箱奖励，但当前客户端房间表没有任何房间引用它；黑涧浴池金箱实际引用 `baoxiang313`。网页按可验证的实际房间链展示，不推测服务器端未公开逻辑。

## 发布影响提示
- 需要重新上传本次生成的 `public/data/parsed/dungeons/` 数据和前端构建产物。
- 数据结构未新增必填字段，旧包回退不受影响。

## 文档同步动作
- ✅ 更新开发日志归档规范与日期索引。
- ✅ 记录源码核查结论、数据异常和验证结果。
