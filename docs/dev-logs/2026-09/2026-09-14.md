# 2026-09-14 开发汇总

## Bug 修复

- 第十四轮（角色池揭晓动画全链路修复与实机时序对齐）：
  - **彻底清除胸前横切黑条**：根因排查定位为 `nameBase`（`spGachaNameDown01.png` 512×512 渐变阴影底衬贴图）。此前无 phaseIndex 门禁且使用了 `right: 50%; bottom: calc(50% + 14px)` 居中偏左定位，从 Step 1 起直接横铺在屏幕中央胸口位置。依据 NGUI prefab 原始定义修正：该节点挂于 `nameRoot(248,-104)` 下局部 `(0,-14)`，`mPivot=8 (BottomRight)`，绝对右下锚点为 `(248,-118)`；改用 `right: calc(50% - 248px); bottom: calc(50% - 118px)` 定位，并严格限定 `phaseIndex >= 2`（仅在 Step 3 伴随角色名牌一同淡入），贴图底部的暗色渐变条精确衬在名字下方，彻底清除切胸黑条。
  - **修复 Step 1 大星被小人/台座遮挡**：台座 `reveal-stage-wrap` 与小人 `reveal-chibi-wrap` 增加 `v-if="phaseIndex >= 1"` 门禁；Step 1 期间仅保留职业大纹章与 `spGachaMeteor01` 大星弹出演出（音效 card2 + card9），不再被台座小人抢占挡住。
  - **修复 Step 2 Q 版小人渲染与时序对齐**：
    - 修复异步 DOM 挂载时机 Bug：此前 `phaseIndex = 1` 触发 DOM 渲染时同步执行 `loadChibi()`，导致 `chibiHost.value` 仍为 `null` 而被短路返回。改为 `await nextTick()` 等待 DOM 挂载就绪后再初始化挂载 Spine 小人。
    - Step 2 期间双层菱形框展开（`card10`）、符文文字环（`spGachaTxtRing01`）旋转，台座与小人在屏幕正中央 `(0, -116)` / `(0, -114)` 现身并播放 `win` 欢呼动画。
  - **实现 Step 3 平滑滑动位移（拒绝瞬移）**：
    - 补齐 `.reveal-stage-wrap`、`.reveal-stage--right`、`.reveal-chibi-wrap`、`.reveal-chibi--right` 与 `.reveal-stars--right` 的 CSS Transform 与过渡动画（`transition: transform 0.5s cubic-bezier(0.12, 0.82, 0.28, 1)`）。
    - 进入 Step 3 时，台座、小人与星级行从中央 `(0, -116)` 平滑滑动至右侧 `(+360, -116)`，右侧灰菱形托板（`spGachaBlock01`）淡入垫在下方，左侧立绘（`heroPic`）平滑微移，名字、职业属性角标与打字机台词完整展开。
  - **换人滑动转场与跳过机制完善**：
    - `nextHero()` 转场动画关键帧修复：此前元素已处于 `+360px`，关键帧误从 `-50%` 起跳导致瞬间拉回中央；修正为从 `calc(-50% + 360px)` 平滑滑向右侧边缘 `calc(-50% + 720px)` 并淡出，Step 3 其他文本及立绘元素添加 `.reveal-fade-out` 平滑淡出（0.45s）。
    - 跳过逻辑对齐 Unity 源码 `HeroGachaShowPanel.Skip()`：若后续抽取结果存在 5★ 角色，走 `ShowRest5StarHeroTime` 逐只重播 5★（开头播放 `get3` 音效）；若无 5★ 则直接进入结算结果页。
  - **自动化端到端测试闭环验证**：使用 Playwright 驱动 Edge 浏览器，通过单抽/十连抽完整生命周期验证（涵盖翻卡、Step 1、Step 2 居中欢呼、Step 3 平滑滑移展开、换人转场、跳过及蜂窝结算网格与关闭返回卡池），截图均已核验无缺陷。
- 第十三轮（用户实机视频逐帧对照：角色池翻卡初始动画）：
  - **等待点击姿态纠错**：游戏流程 = 开场坐姿（`startopen` 2.67s，与 Timeline 完全一致）→ **自动趴到桌子底下**（`startopen_waitclick` 4s 循环，只露头发，触摸继续/点击提示在这一阶段）→ 点击后端着卡牌从桌下起身（common/surprised × one/ten）→ `end` → 直接进揭晓。此前把坐姿 `idle` 当等待状态（第九轮的「实机对照」结论有误），导致点击后「先趴下再起身」的错序（用户指出的 bug）；等待阶段改播 `startopen_waitclick`。
  - **触摸继续贯穿翻卡段**：源码 `SetClickCount` 只隐藏 tail_tip，continueObj 到面板关闭才消失（视频逐帧确认翻卡动画期间仍在），显示条件从「仅等待」改为「等待+翻卡」。
  - **初始动画「桌面重复」**：移除静态桌面图 `gacha_cardforeground_output.png`——它与 `elsa_rawcard_desk` 骨架内容完全重复（实测隐藏后画面零变化），且静态图在开场相机缩放层里而 Spine 不缩放，开场时出现双重桌沿/双份道具。
  - **桌子位置**：游戏桌面在独立前景 3D 平面（prefab `gacha_BG.foreground/gacha_elsa_desk`），终帧桌沿贴画布底边；网页同一相机渲染时桌沿在设计 y≈-310、底下多 65px 暗带（用户标注）。`gachaSpinePlayer` 新增按层 `yOffset`（设计像素，经 `Skeleton.y` 根偏移实现，动画不覆盖），桌面层下移 65。
  - **出 5 星桌面状态**：等待与翻卡阶段桌面层按稀有度播 `surprised`/`common` 循环（源码 `SetCardType` 的 `gacha_BG.SetBool("surprised", !common)` 一次设定贯穿全场），此前固定 `idle`。
  - tail_tip 改 `mix-blend-mode: screen`（游戏为加色微光，常规混合把软边光斑贴图渲染成实心白团）。
- 第十二轮（用户两点反馈：第一个蛋弹出过早 + 蛋大小/位置）：
  - **第一个蛋等袋子开完再出**：源码 `PetGachaAniPanel.WaitShowPet` 的 `time` 序列化值 = **1s**（点开袋子到出蛋的等待；0.36 是蛋自身 DOScale 时长，此前被误当等待值）——`openBag` 的 startEgg 等待从 360ms 回正，实机观感 1s 偏拖，按用户反馈收敛到 **700ms**（实测 600ms 时袋口刚张开、700ms 出蛋正好衔接）；`get5` 音效与 `gacha_show_egg` BGM 移到蛋出现时播（源码同播时机）。后续蛋维持源码 `OnClickShowPet` 的**立即弹出**（open_jump 与出蛋同起，rise 自带 0.14s 延迟作节拍），`nextPet` 由 360ms 改为直接调用。
  - **蛋大小/位置按用户对照截图逐像素实测**（两侧菱形框 352/362px 几乎同大，可直接对比）：游戏蛋宽 ≈164px、蛋心在菱形中心**下方** ≈14px；网页（×1.65）蛋宽 ≈197px、蛋心在中心**上方** ≈10px。显示倍率 **1.65 → 1.4**，落点 **(0,+40) → (0,+10)**（fall/settled ty 240→270）。
  - **蛋池结算数量标注**：`tip-cell__count` 位置/描边按用户实机标注校正（right/bottom 23px，18px #f8eedc + 四向深棕描边）。
- 招募模拟页对照游戏源码（`HeroShowUI`/`HeroGachaShowPanelUI`/`PetGachaAniPanel`）与 prefab 几何转储（`backups/gacha-recon/05-HeroShowPanel.txt` 等）+ 用户提供的实机结算截图与抽蛋视频，集中修复角色池结算、揭晓名称组与蛋池出蛋/结算：
  - **角色池结果一览**：排布由错误的「3-4-3 稀疏菱形」改为 prefab 真实的**蜂窝错半格网格**（中排 4 张 0/3/6/9、上排 3 张 1/4/7、下排 3 张 2/5/8，列步进 140、行距 140、卡 256×256）；卡内元素全部按 prefab 子节点坐标重排（碎片图标 108×108 @(-10,8)、×N @(18,-40)、新角标 60×24×3 @(0,-48) 居中、`gacha_card_reget` 底衬 240×68 @(0,-48) 替换误用的 `gacha_text`、class/element 角标 @(68,-40)/@(102,-6)、星条 @(0,-77)）；结算卡片改为不可点击（游戏 `HeroShowItem` 无交互，此前连点推进会误触跳转图鉴打断流程）。
  - **揭晓演出**：step3 名称/标签组按 prefab 坐标重排（name 右端 (128,-70)、标签排 y=-126、elementTextBase 用原 320×320 贴图 @(104,-126)、elementIcon @(510,122)、classFrame/classIcon @(573,79)，此前整体偏移 ≈+166/+98 导致属性图标被屏幕右缘裁切）；星级行绝对位置修正为 (0,-171)/step3 (360,-171)；step2 时长按星级区分（3★=1.05s，4/5★=3s，此前一刀切 3s 导致十连全 3★ 时拖沓近 20s）；跳过补展示剩余 5★ 时每只播 `get3`；结果页打开不再立刻切 `gacha_shop`（源码关闭结果页才切）。
  - **蛋池出蛋**：修复蛋「变形/撑满菱形」——rise/bounce 关键帧此前直接覆盖蛋图 transform，丢掉 `translate(-50%,-50%)` 居中且把缩放叠到 ≈1.7 倍；改为动画挂在 `.pet-egg__inner` 包裹层、蛋图静态居中，终态 132×138 @(0,-20)（实机视频对照一致）；落位改为轻微回弹（原 bounce 会把整蛋瞬移 -260px 再落回，即用户看到的「向上消失」观感）；旧蛋飞走延长到 0.8s 升到货架高度、承载节点存活到动画结束（原 340ms 提前卸载）；星级/名牌坐标修正为 @(0,-120)/@(0,-163)。
  - **蛋池结算**：遮罩由黑 50% 改为源码口径的**白 50% 冲洗**（`rgba(255,255,255,0.502)`，实机截图核对）；`item_get_titel` 标题条水平居中；结算格改为不可点击（同角色池口径）。
- 结果一览第三轮校正（用户提供 1916×907 实机对照）：屏幕两侧/顶部的浅色软边带确认是 **Rconer/Rconer2 压角层**（1700×1220 浅色圆角框贴图）在宽画布下露出——游戏结算背景组合里没有这两层，已移除；碎片图标按用户标注改为**小拼图图标**（`chara*_p` 素材本身是「头像嵌小拼图」，渲染 108 ×0.5 ≈ 54px，中心 (-30,-40) 紧挨 ×10 左侧），不再放大到卡面中央；`gacha_card_reget` 阴影底衬恢复（240×68 @(0,-48)，压边框、垫角标与碎片，位于角标之下、碎片图标之上）。
- 第四轮（用户实机对照 + 卡死排查）：揭晓殿堂背景同样移除 Rconer/Rconer2 并改为 `inset:0` 整幅铺满（图1 两侧竖向阴影条同源）；结算卡的职业/属性角标改为**仅「新获得」显示**（prefab 中两者在 `new` 组内，实机重复获得卡无角标）；结算背景按实机截图提亮（`brightness(2.1) saturate(0.85)` 对齐游戏 `bg_bottom + bg_toplight` 的亮灰蓝观感）。**卡死根因**：`gachaSpinePlayer.createSpineScene` 每次为图集页新建 `GLTexture` 但 `dispose()` 只释放 SceneRenderer——揭晓每换一个角色泄漏一整张贴图，多次抽卡后 GPU 显存耗尽把整个窗口挂死（无法点击、无法 F12，JS 主线程仍响应，与探测结果吻合）；`dispose()` 现在逐一释放场景创建的 `GLTexture`。已验证结算页空闲 60s+ 主线程稳定（12 动画、37MB 堆内存持平）。
- 第五轮：结算卡补上**碎片溢出转化记忆结晶**的展示（源码 `CheckHeroExcit` case "3"）——`converted` 时图标换 `duplicate.overflow.icon`（回忆结晶 `item_20026`）、数量为转化数 `overflow.count`，与碎片小图标同位（底衬上、×N 左侧）；角色抽卡的跳过按钮从 HUD 视口锚定（109×51 右下角）改为 prefab 设计坐标 **gacha_btn_skip 128×60 @(548,-302)**（随画布缩放），与魔物蛋池（GachaPetPanel）完全一致，翻卡段与揭晓段同步调整。
- 第六轮：**卡死第二层修复**——蛋池「十连 → 跳过 → 关闭结算」必现的整窗挂死：翻卡/蛋池面板每次抽卡新建 WebGL 画布，`dispose()` 释放纹理后上下文仍要等 GC 回收，反复抽卡累积几十个大纹理上下文把 GPU 进程压垮；`gachaSpinePlayer` 新增 `dropContext()`（`WEBGL_lose_context`），翻卡/蛋池面板卸载与揭晓整体卸载时主动丢弃上下文（揭晓**逐角色切换不可调用**——小人画布复用同一上下文）。结果一览舞台改回默认 contain（与蛋池结算一致：窄窗口/开 F12 半屏时蜂窝网格等比缩小完整显示，此前 `fit="height"` 会左右裁掉网格）；结晶（converted）同样垫 `gacha_card_reget` 阴影底衬；解除结算卡与卡内元素的 `pointer-events:none`，碎片/结晶可被浏览器元素选择器（F12）拾取检查。翻卡与揭晓舞台同步从 `fit="height"` 改回 contain——窄窗口下画面完整不裁切（与蛋池口径一致），全宽窗口两种模式构图相同。
- 第七轮（用户实机截图对照，蛋池结算 + 出蛋揭晓）：
  - **蛋池结算遮罩改回黑 50%**：prefab `GetRewardTip.mask`（UISprite #70307，PathID 与脚本字段完全匹配）实为 sprite 名 "white" + 染色 `rgba(0,0,0,0.502)` —— **黑 50% 压暗**，此前把 sprite 名误当颜色改成白冲洗（第五轮结论作废），实机截图（图1）背景为压暗非发白。
  - **品质框修复**：结算格品质取值由 `rank ?? quality` 改为 `quality ?? rank` —— 蛋池 `tierRank` 是 rare1/2/3 保底档位（1~3），此前优先级写反导致 4/5★ 蛋也渲染蓝框。
  - **翼型徽印补进结算**：`petPool.reward → mowuPool_1`（tip「魔物特别招待券_赠品」）= 每抽赠 1 个 `item_20024`，源码 `ResponsePetRouterGacha` 的 `rewardData.reward += data.reward` 把它聚合进 GetRewardTip；`parsed/gacha.json` 已有 `pool.bonus` 字段，`GachaView` 新增 `petSettlementItems`（蛋 + 赠品按抽数聚合一条，金框 + 数量），出蛋揭晓列表保持只有蛋。
  - **结算格样式按用户标注校正**：图标 80×80 @top55%、连体星条 `top:19px; height:34px`（宽度按 com_stars 原始比例自动，删掉按品质写死的宽度）、数量 18px `#f8eedc`（`SetCnt` 的 ColorString[1]）在帧右下内缩 14（140 格右/下各 20px）。
  - **出蛋揭晓按 prefab `04-PetGachaAniPanel.txt` 校正**：蛋显示改回**纹理原生像素**（第八轮更正：源码 `MakePixelPerfect` 会覆盖序列化的 132×138/scale2，蛋纹理 110~155px 不等；264×276 结论作废——这就是用户指的「蛋比游戏大这么多」）；`showPetUI` 组整体在 (0,+20) —— 蛋框 @(0,20)、星行组内 (0,-137)→绝对 (0,-117)、新获得组内 (94,96)→绝对 (94,116)；**star1..5 的 mDepth 递减（12→8）即左边星盖住右边星**，用 z-index 递减复现（此前 DOM 顺序右盖左，与实机相反）；名牌文字用**魔物名**（prefab UILabel 示例「宝石迷迷可」），蛋候选名去掉「的蛋」后缀；名牌绝对位 (0,-163) 维持 prefab 值。
- 第九轮（用户视频逐帧对照：出蛋轨迹 + 结算数量）：
  - **数量样式再校正**：游戏里 "10" 在**框内右下角**（右缘内缩 ≈10px、下缘内缩 ≈15px，140 格坐标 right:16 / bottom:20）——上一轮依据的放大图实为**本站页面**而非游戏，据其改到「悬挂框角外」是误判，已回正；颜色按源码 `SetCnt` 的 BBCode `[ColorString[1]]` = **#f8eedc**（Const.cs）+ cnt UILabel NGUI Outline 描边 `mEffectColor` rgb(23,14,7)。
  - **出蛋轨迹按源码坐标语义修正**：蛋运行时被回挂 `Inbag`（clip 410×410 @（0,240)）下，TweenPosition (0,-360)→(0,+40) 是 **Inbag 局部坐标 = 世界 (0,-120)→(0,+280)**——蛋从袋身后（裁剪区外不可见）**弹到接近货架的高点 (0,+280)**（用户指的「很高的弹出高度」），rise onFinished 回挂 outbag 后**落进菱形框 (0,+40)**。此前把 (0,+40) 当世界坐标 → 蛋贴着袋口弹出、高度过低。实现：裁剪区容器（overflow hidden）+ rise（ty 400→0）/ fall（ty 0→240 带轻弹）两段动画；ShineEft 光晕是面板级粒子不在裁剪区内，挂在高点、落位后淡出。
  - **旧蛋瞬时消失**：源码 `OnClickNext` 是 `SetActive(false)`——瞬时隐藏、无飞走动画（此前自加的 0.8s 向上飞走已删）；下一只蛋 `time`=0.36s（prefab 序列化值）后从袋口弹出。
  - **星级时序**：`.pet-star__shine`（闪光层）此前 `both` 填充把 from 的 opacity:1 反向填充到延迟期——星星未轮到就整颗亮着、再播消失动画（用户指的「星级先出来」）；改为初始 opacity:0 + forwards。星级起始延迟 0.10→0.35s（蛋落位、菱形/名牌先出，星级再逐颗弹），新获得角标延后到 0.8s。
  - **第十一轮（用户三点反馈）**：
    - **第二抽起无动画直接弹结果**：`resume()` 引用了 `init()` 内部的 `frame` 函数——共享场景第二次 acquire 时 resume 抛 `ReferenceError: frame is not defined`，被面板 onMounted 的 catch 静默吞掉后走 finishAll 直达结算。修复：`frameRunner` 提升到 createSpineScene 作用域，init 里赋值、resume 引用（`!frameRunner` 时跳过）。已回归：第二抽袋子动画正常。
    - **菱形框出现时机**：uiVisible 从「蛋落位后」提前到「蛋开始下落时」（fall 起点）——蛋下落的 0.35s 里菱形/名牌正好淡入完成，落到时刚好加载好（用户逐帧对照）。
    - **蛋大小**：真机上 MakePixelPerfect 会乘 UIRoot pixelSizeAdjustment，实机截图（图2）蛋占菱形宽约 6 成——蛋图改按原生像素 × **scale(1.6)** 显示（此前 1.0 偏小、早前 2.0+fill 拉伸偏大且变形）。
    - **新获得角标**：确认 `gacha_new.png`（HeroGachaShowPanel_Atlas）资源在位且渲染正常（pet-new @ (94,116)、延迟 0.8s）——仅在**新获得**的蛋上出现（源码 `CheckNewEgg` 口径），重复获得不显示；用户此前连续抽到重复蛋所以没看到。
  - **第十轮（用户截图两连）**：
    - 落位蛋被裁剪区切掉下半——源码 rise onFinished 把蛋**回挂 outbag（无裁剪）**，fall/settled 阶段不该留在 Inbag 里；改为按阶段换父：rise 在裁剪区（205,165），fall/settled 移出裁剪区、从高点 (0,+280) 由 fall 动画落进菱形框。
    - 蛋要和袋子打开**同时**出来（用户视频对照：游戏里蛋在袋子还在开时就弹出）——openBag/nextPet 的 startEgg 等待 360ms→120/150ms（rise 自带 0.14s 延迟作节拍），蛋在 open_X/open_jump 过程中弹出；下一只蛋同样与 open_jump 同起。
- 第八轮（用户实测反馈：连续抽卡整窗卡死 + 结算格/出蛋细节）：
  - **卡死根治——共享演出场景**：第七轮的 `dropContext` 方向不对——每次抽卡新建/销毁 WebGL 上下文本身就是问题（上下文要等 GC 回收，反复建/丢累积待回收显存把 GPU 进程压垮，用户实测几次抽卡后整窗挂死需刷新）。`gachaSpinePlayer` 新增共享层：`mountSharedSpineScene/releaseSharedSpineScene`（翻卡/蛋袋场景按 key 复用同一画布、上下文、纹理与解析好的骨架；引用归零只暂停渲染循环；视口比例变化超 2% 才整场景重建）、`mountCanvasScene/pauseCanvasScenes`（揭晓小人**按角色缓存**在共享画布上——同一角色再次揭晓零上传零解析，切换角色时暂停其他场景并清掉残留末帧）、`disposeSharedSpineScenes/disposeSharedCanvases`（只在离开 `/gacha` 时统一销毁，`GachaView.onBeforeUnmount` 调用）。翻卡/蛋袋/揭晓三处面板全部接入。压测：角色池连续 8 轮（含单抽）+ 蛋池 5 轮，rAF 全程正常、单轮耗时无劣化、JS 堆稳定（48~54MB）。
  - **结算格星条只给魔物蛋**（源码 `ItemBagCellUI.CheckPetEggItem` 仅 `isPetEgg` 显示；翼型徽印等非蛋道具不显示星级——上一轮误给所有结算格加了星条）。
  - **数量样式按源码 + 实机截图度量**：18px、#f8eedc（`SetCnt` 的 ColorString[1]）+ **深棕描边**（cnt UILabel `mEffectColor` rgb(23,14,7)，NGUI Outline 用四向 text-shadow 等价——此前只有软阴影无描边，观感差异即用户指的「颜色没用地原版」）；间距按实机度量右缘内缩 ≈8px、下缘 ≈4px（140 格坐标 right:14px / bottom:10px）。

## 重构与维护

- 精简导航上传流程：`App.vue` 临时切换为 `NavigationMenuLite`（props 与完整版一致），全量提交后推送 GitHub，再把 `App.vue` 还原为完整版 `NavigationMenu` 并保留在本地工作区不提交。
- 本次提交（`bef72d3`）落库了此前长期未提交的变更：暂存区已有的仓库整理（`node_modules/`、`dist/` 移出跟踪）、图片归档迁移，以及招募/符石/家具/设施/伙伴邮件/吉祥物等功能代码、`parsed/` 数据、文档与测试。临时文件 `.html`、`.tools/`、`pet-repro.tmp.mjs`、`theme.css~` 排除在提交外。
- 远程分叉处理：远程 main 存在 8-29 旧快照（「ui调整」）与 9-1 网页删除操作两个本地没有的提交。直接合并会把 433 个本地已删除文件（`public/data/` 原表、归档日志等）重新带回，故两次均用 `git merge -s ours` 只接历史、内容以本地为准；推送完成（`db2303d..3d10331`）。
- 新增 `docs/ARCHITECTURE.md`「Git 提交与推送约定」：明确本地提交为存档点、快照含图片可精确恢复；提交时机（动手前/说得清的改动完成/实验前/日终）、代码与数据重建分开提交、临时文件进 `.gitignore`、图片先提交再动工并按 SPEC 备份原图、已推送历史不得改写、远程旧快照分叉用 `-s ours` 合并。

## 验证

- 浏览器（1600×900，dev :5199）第十三轮回归：开场坐姿 2.67s → 自动趴桌（发球+触摸继续，同视频 4.9~5.8s）→ 点击后从桌下起身抱卷轴（同视频 6.8s）→ end 自动进揭晓，全链路截图逐帧对照视频一致；桌子下移后桌沿贴画布底边（暗带消失），`vite build` 通过。
- 浏览器（1600×900，dev :5199）第十二轮回归：点开袋后 600ms 截图——袋口仍在打开、蛋未出现（时序修复生效）；落位截图蛋居中略偏下、与菱形框比例同实机对照图一致。
- 浏览器（1600×900，dev :5199）第七轮回归：蛋池十连截图对照——揭晓（蛋不挤压、名牌「秋风妖精」无「的蛋」、星级行位置与左星盖右星）与结算（蓝/紫/金品质框、翼型徽印 ×10 金框、数量 18px 米白右下、黑 50% 遮罩压暗观感同实机图1）全部一致；结算 mask 点击两段式关闭正常。
- `vite build` 通过；浏览器（1600×900，dev :5199）回归：角色池十连蜂窝网格/卡内几何/揭晓名称组截图对照 prefab 数值一致，蛋池出蛋落位与飞走、白 50% 结算遮罩与居中标题截图对照实机一致；结算卡片点击不再跳转图鉴。（第七轮把白遮罩更正为黑 50%，见上）
- 切换精简导航后 `vite build` 通过，随后才提交。
- 推送被拒一次（远程 9-1 有新提交），fetch 后追加 `-s ours` 合并再推，成功。
- 招募修复改动未运行 `npm run verify` 全量（仅 UI 组件与视图改动，不涉及数据管线；`vite build` 已通过）。
- 文档改动未运行 `npm run verify`（仅新增 Markdown 章节，不涉及代码与数据）。

## 发布与回退

- 无需构建与热更包：本次推送仅为仓库同步，导航在提交前已验证构建，推送后本地已还原完整导航（`src/App.vue` 为唯一未提交改动，属预期状态）。

## 未完成事项

- 角色池结果一览的背景：prefab 是组合结构（bgLeft/bgRight 1000×1000 镜像 TextureLoad + bg_mid/bg_front 侧柱 + bg_bottom + bg_job 纹章），TextureLoad 的运行时贴图在 dump 中无引用，现仍复用揭晓 `bg.png`；待实机结算截图对照后再定。

## 文档同步

- 已更新 `docs/ARCHITECTURE.md`（新增第 7 节）、`docs/README.md`（ARCHITECTURE 简介补充 Git 约定）、本日报。
- 已更新 `docs/SPEC.md` §9.8：结果一览真值改为蜂窝网格与卡内 prefab 坐标（推翻 09-13「3-4-3」结论）、揭晓 step2 时长与名称组坐标、蛋池结算白遮罩与出蛋动画写法。

## 抽卡动画与揭晓面板像素级精修（对照原版视频抽卡111.mp4与源码）

1. **开场抽卡桌面与艾尔莎趴桌悬空修复（图4/图5）**：
   - **桌子过短留黑边**：Spine 中桌子 `desk` 只有 1680px，因 `zoom: 1.2` 视野缩小后宽度仅 1401px 露底；配置 `stretchX: 1.25` 配合 `.card-desk-fill` 全宽底色层铺满横屏。
   - **趴桌尾巴悬空 25px**：原版 Spine 在 `startopen` 2.33s 时 `basebody` 重置归零，前人仅给 `desk` 加了 `yOffset: 65` 而未给 `elsa` 加，导致垂直错位 65px；两层骨架同步加上 `yOffset: 65` 后，尾巴直接落入桌面，与原版完全对齐。

2. **抽卡素材全链路预加载（彻底解决图标加载过慢与闪烁）**：
   - 新建 `src/utils/gachaPreload.js`：卡池挂载时静默预加载全部静态 UI（背框、星星、底板、图腾、职业徽标、属性角标等）；
   - 用户点击抽卡瞬间，在进入翻卡页前直接在后台预热所有抽中角色的立绘和卡面，进入揭晓与换人时 0 延迟。

3. **图2 vs 图3 实机差异修复（拉碧丝 Lapis 及全角色）**：
   - **职业图标与图腾**：
     - Step 1 图腾：Job 6（支援）更正为带翼十字架 `chara_bg_class_6.png`，深青灰色滤镜对齐实机；
     - Step 3 台座徽标：Job 6 采用带翼十字架 `gacha_class6.png`，坐标由 `(213, 79)` 纠偏至 `(195, 75)`，完美入凹槽。
   - **台座正面星星**：尺寸由 48px 扩大到 66px，外加 `margin: 0 -4px` 微重叠与立体阴影，饱满填满台座正面。
   - **Q 版小人相机尺寸**：视口扩大到 `viewportHeight: 2800, groundY: 282`，画布增大至 440×520，高个、高帽子及浮空小人头部翅膀 100% 完整，双脚稳立台面。
   - **托板顶部凹槽穿模**：3★ 显示 `midFrame`（`chara_bg_center_only`），遮蔽后方线条，消除穿模。

4. **3★、4★、5★ 揭晓立绘与演出差异化全面实现**：
   - **3★**：银白外框 + 属性光晕，显示 `midFrame`，隐藏旋转符文文字环，时长 1.6s。
   - **4★**：`star4FX` 青蓝霓虹框外发光 + 闪烁星芒粒子 + 匀速旋转青蓝符文文字环，时长 1.9s，音效 `card12`。
   - **5★**：`star5FX` 火红炽热光环 + 巨幅火焰流星横穿全屏 + 旋转火焰符文文字环 + 台座表面 `star5shine` 金色烈焰圣轮，时长 2.25s。

