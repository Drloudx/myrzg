# 2026-09-09 开发汇总

## UI 与交互

- 营地 UI 重设：建筑改为七张图标选择卡，升级档位并排显示当前/升级后外观、效果，条件、材料和奖励分区；等级筛选移入正文，默认第一级，保留全部等级入口。属性研究移除下拉框，按前置关系生成三组可点击研究树，接入两张游戏原始底板，点击进入对应等级详情、返回保留分类；搜索保留祖先路径。研究树从左向右推进，同层分支上下排列，手机图内横向滚动、初始定位左侧起点，页面根透明、正文加 paper-panel 边框。节点恢复原底板 292×88 比例，方形图标不挤压，名称和总等级放在素材横线上方、描述在线下方；连线同步使用节点尺寸。详情返回按钮移至右上角。
- 营地 UI 验证：相关单元 7/7 通过；营地/原设施/来源桌面手机回归 24/24 通过。视觉检查发现手机树初始根节点偏右，居中修正后营地专项 10/10 再通过。两张新增贴图与原素材 SHA-256 相同，查看树、建筑首屏、研究详情及明暗截图；最终 Vite 构建通过，未做 Android 真机验证。

- rune-appraisal 合成布局修订：手机鉴定操作栏改为独占一行、左对齐。合成移除目标下拉框，采用符石列表同款卡片，显示产物、等级/部位、效果前后对照和两列材料；支持等级/部位筛选、效果搜索、20 项懒加载与来源方案优先定位。按追加反馈删除合成次数，旧 count 链接也只展示单次合成。兑换页移除 Gem 分类和条目，旧分类链接回落有效分类；保留解析所需原表。次数箭头抽到 RuneCountInput 供鉴定使用，修正本页深色激活筛选文字对比度。符石桌面/手机 UI 20/20 通过，追加兑换回归 2/2 通过，查看明暗截图。

- rune-appraisal 本轮：移除物品详情重复图鉴按钮，三个符石页签正文使用魔物收益同款边框；鉴定选中物品复用品质框并可点开详情，选择其他展开其余八种，增加上下箭头次数框和实际配置模拟结果。同名结果合并数量，详情返回保留最近批次，切换方案清空。按追加反馈，概率固定两位小数，手机端概率、结果及选择列表均为两列；修正深色选择按钮对比度。
- rune-appraisal 概率核查：九个奖励配置与解密表逐项一致。小型/元素 I 等级合计为 94/5/1，中型/元素 II 为 30/60/10，大型/元素 III 为 II/III 各 50%。源码实际结果经 ItemBagMsg 从服务器返回，网页按原始权重独立有放回模拟；完整证据与边界写入 RUNE_CATALOG，不添加内部诊断标签。
- rune-appraisal 本轮验证：相关单元 49/49 通过，两位精度调整后符石及共用规则 30/30 通过；最终桌面/手机符石与奖励详情 UI 26/26 通过。初轮两项测试因 innerText 与 textContent 的隐藏名称差异失败，统一可见文本断言后重跑通过。额外检查 344px 窄屏，33 张概率卡无横向溢出；查看选择、结果、明暗及滚动截图。末次按钮配色修订后 Vite 构建通过，未做 Android 真机验证。

- 设施功能按符石页修订统一搜索栏、页签、筛选和计数顺序，覆盖普通配方、营地升级、属性研究；营地移除额外包装层以接入公共吸顶裁剪，正文背景不再铺到页面根。切换页签复用真实滚动根，保留具体配方来源定位；错误与加载状态仍可切换页签。
- 符石列表按反馈移除「查看详情」「合成下一级」两个文字按钮，保留图标点击详情、合成页签及获取途径链接，同时删除未再使用的快捷合成映射和事件函数。

- gacha-animation 对照游戏横屏截图再次校准：左上完整招募标题不再裁断，UP 概率提升改用 `gacha_up_botm.png` 阴影和 `at_f_5.png` 头像边框；重调池页签、右侧标题、计数、底部保底和抽卡按钮的横屏百分比定位，保留原图装饰与阴影。

- 符石图鉴按反馈修正筛选头：页签移到搜索栏下方；「共 X 种」与事件/兑换页统一为 13px、600 字重和次要文字色。移除根容器实色背景，仅正文保留纸色，修复桌面滚动时吸顶栏上方露出的实色条；切换页签通过公共滚动目标解析器回到正确起点。

- gacha-animation 视频修订：按七张参考图与 90 秒视频修复主界面对齐、原图按钮悬停重置及整图拉伸。按钮按原图集左右 60px 九宫格显示，按下使用原 `_press` 素材；作用范围只在招募模块。
- 角色揭晓增加职业星级、原 Q 版 win/win_idle、石台/菱形框、立绘入场、元素职业与原招募对白；十连使用 3-4-3 菱形卡面，重复角色使用原记忆碎片图标。魔物揭晓使用原蛋图/名称/星级底板，结算为横向奖励层并包含附赠兑换币；隐藏底层操作栏避免按钮重叠。

- gacha-data：新增招募模拟 `/gacha`，包括两个角色池、两个魔物蛋池、原图封面、单抽/十连、结果卡、概率详情和最近 200 条记录；支持种子重置、初期保底模拟选项及本地记录恢复。概率/结果/设置继续复用全站组件与主题。
- gacha-animation：按用户实际截图改为全屏游戏原背景、大立绘、左侧头像池选择和红/青招募按钮；仅招募路由隐藏 Wiki 三栏，手机竖屏重新布局。接入原角色/桌面及魔物袋 Spine 4.0 动作、原 BGM/音效、逐个揭晓、跳过和重播；只读已结算批次，离页、取消加载和减少动态效果均不重复发奖。

- 设施功能新增营地升级、属性研究两个页签，包含 7 类建筑及 38 项研究；支持建筑、研究分类、等级和搜索筛选，前置研究、材料详情、改良配方及家具外观跳转。保留旧设施配方与 `tier/item` 参数，同路由前进后退以 URL 恢复。
- 营地正文使用实色主题阅读区，研究图标使用游戏原图；深色激活筛选文字使用固定前景 token，手机端奖励长名称换行并增加浮动导航底部避让。

- 物品和装备详情的奖励区统一为 `AcquisitionRewards`，补充上锁宝箱钥匙与符石鉴定消耗；自选奖励不再逐项标注必得，随机装备可展开候选列表。未新增独立玩法页或内部来源诊断文案。
- 物品/装备获取途径补充分组：随机事件、探索、采集、种植、营地升级、活动、签到、章节首通和新手引导。种植来源可打开种子并沿现有详情栈返回；事件/探索和怪物来源定位具体详情，营地来源对接设施分支的建筑及升级前档位。
- 修复探索来源直达事件页时仍使用随机事件模板导致详情空白的问题，筛选写回保留叠加的 itemId。
- 角色图鉴的技能升级材料与角色突破材料改为可点击按钮；点击后保留当前角色详情和筛选参数，并叠加打开对应物品详情。
- 材料按钮补充悬停、键盘焦点和“点击查看物品”提示状态。
- 材料按钮悬浮状态统一复用好感礼物的上浮与阴影效果。
- 技能等级选择改为单轨双滑块，点击轨道可直接定位目标等级，两个圆球仍可独立拖动。
- 保留 `NavigationMenu` 作为当前默认完整导航，新增 Git 上传版本备用的 `NavigationMenuLite`。精简版只展示角色图鉴、魔物图鉴、菜谱查询、魔物收益和成就查询；所有页面与路由保持不变。

- 招募模型主界面继续按游戏横屏截图校准：放大左上招募标题，调整左侧卡池按钮图标与文字比例、顶部资源胶囊宽度、底部概率/记录按钮尺寸；消耗提示改用 `gacha_btn_tag.png` 原始阴影。抽卡按钮恢复 `292×72` 原图比例并保留图集规定的 60px 左右九宫格切片，避免两侧箭头拉伸。
- UP 概率提升卡改用 `HeroPoolPanel_Atlas` 中的 `gacha_at*.png` 招募头像横幅（不再使用整张角色卡立绘），保留 `at_f_5.png` 品质边框，并将概率提示与头像横向排列。

## 重构与维护

- gacha-animation 视频修订：新增 `GachaHeroReveal/GachaSettlement` 和独立 `gacha-presentation.json`，保持随机逻辑/结果顺序/存档不变。资源清单扩展为 291 文件约 124.0 MiB，包括 33 个正式角色骨骼、原立绘/碎片和魔物蛋。演出计时器与 GPU 资源按角色生命周期释放。

- gacha-animation：固定官方 `@esotericsoftware/spine-webgl@4.0.31`，演出时懒加载。新增原资源导入脚本与 SHA-256 清单，复制 66 个文件约 67.4 MiB（主要为原 WAV，图片不压缩）。补全桌面图集第二页，统一 PMA 上传避免透明区 RGB 扩边显色；可取消传输、纹理、RAF、音频按生命周期释放。沿用公共模态骨架，修复沉浸页桌面 sticky 标题遮挡页签及视口限高问题。

- rune-appraisal：实现 `/runes` 符石图鉴，含符石列表、鉴定、合成三个页签；114 枚符石、9 类鉴定、95 个合成方案，支持等级/部位/效果筛选、批量计算、物品详情及下一等级合成跳转。完整导航在装备图鉴后增加入口，不修改精简导航。
- rune-appraisal：截图检查后将完整符石名放入条目正文标题，缩略图只保留原生品质框和图标，避免短名称板截断名称导致种类难以辨识。
- rune-appraisal：`runeData.js` 统一效果、原表合成映射和反向来源，`itemParser.parseRuneEffect` 复用该实现；鉴定/合成和物品/装备详情共享 `AcquisitionRewards`，增加可选 `costTitle`。`scaleAcquisition` 区分固定产物数量与随机抽取次数。
- rune-appraisal：搜索构建增加 `runeAppraisal/runeSynthesis` 来源，替换相同方案的重复 Gem 兑换来源，保留原兑换页；所有方案与筛选由 query 恢复，材料叠加详情不清空上下文。

- gacha-data：新增 `gachaData.js` 构建解析、`gachaSimulator.js` 纯抽样事务、`useGachaSimulation` 独立存储和 `GachaResultCard`。随 `data:build` 生成 `parsed/gacha.json`，约 55 KB；结果批次接口可直接供动画分支消费，不在播放/跳过时重新抽奖。

- 营地新增 `campFacilityData.js`，由设施预解析脚本输出 42 个建筑等级、35 次升级、171 个研究等级；`facilities.json` 原有配方数组末尾追加营地对象，同步运行时校验。
- 营地源码核对：建筑消耗/门槛/奖励取当前级，升级效果取下一级；末级费用不展示。建筑属性取当前级、建筑能力逐级累加，同一研究效果不累计历史等级。图标原样复制 38 张，没有压缩或覆盖已有素材。
- 营地配置差异记录在 `docs/CAMP_FACILITIES.md`，包括中心 5 级装饰上限、货运站 3 级累计载重、冷却研究单位及要求中心 8 级的研究；页面不显示内部审计提示，不伪造缺失等级。

- `acquisition-rules`：新增纯规则模块，统一奖励组、固定装备、装备候选池、货币、使用消耗及概率文案；旧 `gameMappings` 接口改为重导出，`parseItemRewards` 保持兼容。锻造与普通设施产出接入共用解析，不改其他分支的来源类型与页面路由。
- 奖励类物品在构建期生成 `item.acquisition`，供后续符石鉴定页面直接消费；抽奖保底与重复转换仍归抽奖数据分支，不从通用奖励表推测。
- source-completion 新增独立来源接入模块，保留现有奖励规则和其他分支编辑；644 件可见物品中，结构化来源覆盖从 388 件增至 449 件，61 件首次获得结构化来源。新增关系共涉及 160 件物品，不把运行时制作来源纳入此统计。
- 完整导航支持通过 `items` 属性复用三种菜单布局，精简导航仅维护自己的 5 个入口。
- 项目根 `raw/` 使用 Git 忽略规则，且当前没有已被 Git 跟踪的 raw 文件。

## 验证

- 符石列表去除文字操作后桌面/手机 16/16 回归通过，确认图标仍可打开详情、关闭后筛选保留，合成页签及来源定位不变；已查看最终两种视口截图，Vite 生产构建通过。

- 设施筛选头修订：桌面/手机设施、营地及锻造相关回归 20/20 通过，覆盖三页签位置、13px/600 计数、滚动裁剪边界、切换回顶、研究前置、配方来源、详情返回及错误态；已查看桌面营地与手机研究滚动截图。前端 Vite 构建通过，本次未改原表、未重建数据或执行 Android 真机验证。

- 符石筛选头修订：新增跨页面计数样式、搜索/页签顺序、桌面裁剪边界和切换后滚动位置的回归；修复前桌面/手机均复现计数为 16px/400 而参照页面为 13px/600。修复后符石页桌面/手机 16/16 通过，已对照滚动前后截图，前端 Vite 生产构建通过。本次不改原表或奖励规则，未重建数据、未做全站 UI 或 Android 真机验证。

- gacha-animation 视频修订：24/24 抽样与演出单元测试通过，含全部正式候选素材存在性/职业元素/对白及原资源哈希。开发服务逐一验证 33 个角色 win/win_idle、非空画布及动态像素，全部通过。
- gacha-animation 视频修订：生产预览 4189 上三组 Playwright 共 26/26 通过，覆盖桌面/手机、原开场、三阶段揭晓、五星单抽、存档不重抽、九宫格按钮 hover/focus/active、3-4-3 顺序、附赠币及再招募。最终截图发现底层按钮显式 visibility 抵消父级隐藏，改为整栏 opacity=0 后，重新构建并补跑视觉交互 4/4 通过。已查看主屏悬停、普通/五星角色、魔物揭晓及两类结算截图；产物在 Codex 可视化目录 `gacha-video-final` 和 `gacha-final-overlay-verified`。未做 Android 真机或全站回归。

- gacha-animation：`node --test tests/unit/gacha.test.mjs tests/unit/gacha-animation.test.mjs` 23/23 通过；生产预览 `4189` 上两组 Playwright 共 22/22 通过，覆盖桌面/手机、参考横屏、原骨骼非空及动态像素、普通十连和五星单抽、逐张顺序、失败/跳过/重播/快速点击/离页存档不变及原数据交互。`npx vite build` 通过。截图位于本次 Codex 可视化目录 `gacha-production-verified`；未做全站回归或 Android 真机验证。

- rune-appraisal：新增 8 项符石单元测试，当前全量单元测试 117/117 通过；桌面/手机符石页与既有奖励详情回归 22/22 通过，含全列表滚动、图片加载、等级/部位筛选、方案定位、批量边界、详情返回、来源跳转及失败重试。初轮 4 项来源测试使用了错误的 CSS 定位器，修正后整组重跑通过，未放宽断言或超时。
- rune-appraisal：完整数据及 Vite 构建通过；首次沙箱构建在清理 `dist/assets` 时遇到权限限制，获准重试成功。已查看桌面、手机及深色截图；未执行全站 UI 和 Android 真机验证。

- gacha-data：抽奖 19 项、资源加载 13 项，合计 32 项单元测试通过；覆盖可见池、开放边界、图片引用、基础概率、单抽/十连一致性、保底、UP、转换、状态恢复及 20,000 抽分布检查。`npm run build` 完整构建通过，末次本页暗色可读性修正后 `npx vite build` 再次通过。
- gacha-data：最终静态预览 `http://127.0.0.1:4189` 上桌面/手机共 10 项页面测试通过，包含抽奖、刷新恢复、池间隔离、物品详情返回、重置确认、错误重试与暗色文字颜色断言；已查看桌面/手机截图。开发服务测试曾在导航/关闭阶段超时，不算通过，以末次生产预览回归为准。未执行全站 UI 回归。

- facilities-camp：营地及既有设施的单元测试 6/6 通过；本次运行全量单元测试 106/106 通过。最终桌面/手机专项 UI 回归 12/12 通过，覆盖升级奖励、满级、研究效果、前置定位、配方跳转、历史恢复、搜索空态与错误态；已检查两种视口及深色截图。并行构建期间一轮出现 4 项页面加载超时，未放宽断言，单 worker 原超时设置重跑全部通过。
- facilities-camp：设施派生产物与当前构建函数输出一致，运行时结构校验通过；38 张研究图标与原资源 SHA-256 全部相同。独立目录 Vite 生产构建通过；本任务未单独执行全量数据重建、全站 UI 或 Android 真机验证，避免与其他并行任务覆盖生成文件。

- 统一奖励规则新增 16 项单元测试，覆盖零值、权重、数量区间、自选、装备池、鉴定消耗与全部 9 类真实鉴定表；当前工作区全量单元测试 106/106 通过。
- `npm run build` 完整通过。首次在沙箱内写入副本生成数据遇到访问错误，获准重试后恢复全部产物；构建期间一项资源目录测试读到中间状态，构建结束后完整重跑通过。
- 奖励详情、现有设施与锻造台的桌面/手机 Playwright 回归共 18/18 通过，覆盖物品/装备入口、自选奖励点击及返回、鉴定银币消耗、宝箱钥匙和设施定位。
- source-completion 及设施、家具、锻造相关解析回归 19 项通过；`npm run build` 完整数据和生产构建通过。
- source-completion 的事件/探索定位、营地奖励及具体建筑档位跳转、种子返回、装备共用来源在桌面/手机最终复测共 8 项通过；使用独立临时输出目录避免与其他分支测试产物相互干扰。未执行全站 UI 回归和 Android 真机测试。
- 角色技能升级材料打开物品详情的 Playwright 回归用例在桌面与手机视口共 2/2 通过。
- `npx vite build` 通过；备用 `NavigationMenuLite.vue` 单文件组件语法编译通过。
- 招募模型调整后 `npm run build` 完整数据预处理与 Vite 生产构建通过；已检查 1920×915 桌面和 390×844 手机截图，确认 UP 头像资源、消耗阴影及抽卡按钮九宫格加载正常。未执行全站 UI 回归或 Android 真机验证。
- 精简导航清单核对为 5 项，`App.vue` 仍有两处完整导航引用且未引用精简版；15 个业务页面路由保持不变（另有根路径重定向）。

## 文档同步

- 研究节点排版同步 `UI_COMPONENT_LIBRARY.md` 和 `CAMP_FACILITIES.md`；本次桌面/手机研究专项 Playwright 4/4 通过，已查看手机研究树和桌面研究详情截图。未重复执行全站构建或 Android 真机验证。

- gacha-animation 视频修订：更新 `GACHA_ANIMATION/SPEC/ARCHITECTURE/UI_COMPONENT_LIBRARY` 的原生结算、三阶段角色揭晓、演出数据、九宫格按钮和验证说明；仍明确 Unity 相机/粒子材质不是逐帧移植。

- gacha-animation：新增 `GACHA_ANIMATION.md`，同步数据交接、SPEC、ARCHITECTURE 和 UI 组件规范，说明原 Spine/网页过渡/未移植 Unity 后处理的边界及发布授权要求。

- rune-appraisal：新增 `RUNE_CATALOG.md` 说明正式配置、源码、内部停用条目和定位协议；同步 SPEC、ARCHITECTURE、UI 组件规范及统一奖励接口，并在物品来源审计中注明符石机制已实现。页面不增加内部审计标签。

- gacha-data：新增 `GACHA_DATA.md`，同步 SPEC、ARCHITECTURE 与 UI 组件规范。内部文档记录引导/关闭池、魔物蛋跨档引用、旧活动文案与时间表差异、客户端/服务器证据边界和动画接口；不在页面显示测试来源或无正式来源审计标签。

- 营地页同步 `SPEC.md`、`ARCHITECTURE.md`、`UI_COMPONENT_LIBRARY.md`，新增 `CAMP_FACILITIES.md` 记录源码规则、内部差异、文件边界及跨任务来源链接协议。

- 补充 `docs/ACQUISITION_RULES.md` 公共接口与符石分支交接说明，同步 SPEC、ARCHITECTURE、UI_COMPONENT_LIBRARY 中的规则入口、预解析字段与组件协议。
- 来源接入规则同步 SPEC、ARCHITECTURE；新增 SOURCE_COMPLETION_HANDOFF.md，记录统计口径、跨分支接口和不能上页面的待确认来源。
- 修正 README、SPEC 与 ARCHITECTURE 中页面数量、路由清单及目录树缩进，使其与当前 `src/router/index.js` 和 `src/views/` 一致。
- `git check-ignore` 确认 `raw/item.json` 命中项目根 `/raw/` 规则，`git ls-files raw` 为 0。

## 未完成事项

- gacha：原 Spine 演出已接入；Unity 相机、后处理、粒子及专用材质尚未逐帧移植。服务器随机抽样源码缺失，初期保底区间解释、同抽多档保底优先级仍为模拟约定。上线前需确认 Spine 和原游戏美术/音频授权，并验证 Android 真机与热更包体。

- source-completion 暂缓通行证 S0/S1 赛季入口、旧 signIn 表和停用活动来源；没有正式入口证据时不写入页面。全量无来源/测试引用审计仍由 source-audit-doc 分支负责。
- Git 上传前需要按发布目标显式将 `App.vue` 的完整导航引用切换为精简导航；当前不自动切换。
