# 2026-09-10 开发汇总

## UI 与交互

- 希尔钓鱼补齐凳子/道具出现、侧身坐下、伸手取竿、放线看漂，以及从咬钩姿态回稳、收线、放回鱼竿、松手起身、走开和道具消失。鱼竿始终为同一节点，`hilFishingMotion` 按竿身位置计算滑动握点及线端轨迹，手与竿、鱼线与浮漂连续连接；循环新增右手调整握姿和看漂视线。思考拆为抬臂、托腮、偏头三个进入阶段，停留时轻动食指/拇指和视线、渐显问号，退出分为收手和回正头部。新增细节与原分段共用暂停、后台冻结、减少动态效果及最后选择队列；修正手指样式优先级导致暂停失效的问题。

- 按用户反馈只精修希尔：原思考仅切换静态托腮手形，现重建肩/肘/手掌分组，连续抬手、托腮停顿、放回身侧，问号与托腮阶段同步。新增秋千和钓鱼：屈膝坐姿、握绳手指、木纹座板与支架、同悬点摆动和晃脚；钓鱼有握竿、绕线手腕、线轮、连续鱼线、浮漂咬钩、竿身弯曲提起、小鱼、水纹和岸边饵盒。动作按钮改公共 UiPopover 点选，其他六人不扩展场景。
- 希尔各场景共用原 SVG 脸、服装、挎包与三节辫，通过受 Vite 更新追踪的源码导入构成新图形；专用场景和活动关节分别在 `HilMascot.vue`、`HilFigure.vue`，动画集中在 `hil.css`，没有常驻逐帧脚本。暂停状态覆盖新增前臂/摆架/线轮等所有动画分组。
- 希尔左右手进一步收敛到共用 `MascotArm.vue`：固定长度上臂/前臂、镜像手套及相同袖口/肩饰，通过 `mascotRig.js` 配置肩点、握点、弯肘方向与握法，配色和长度可供后续角色适配。待机、思考、秋千、钓鱼均保持同一人物比例，取消坐姿额外缩小；画布统一 320×400，桌面宽 192px、矮窗口宽 144px，选择预览同步复用新模型且关闭动画。水潭向右收拢，脚、木桩与水面之间留出干岸，水纹和溅水限制在场景内，鱼线与浮漂仍连续连接。当前仅希尔接入共用手臂，未扩展其他角色。
- 希尔秋千接入完整进出场：道具出现、靠近、握绳、坐下、轻荡；离开先减速，再松手跳下、落地缓冲、走开和收起道具。站姿/坐姿共享 `MascotLeg` 髋膝关节和靴子，`useMascotSequence` 执行浏览器关键帧，`useHilChoreography` 串行调度，人物节点不重建，连续点选只记最新目标；思考切走先收手、回正头部。暂停/后台冻结分段，减少动态效果立即落到静态终态，已暂停时选择新动作直接显示静态姿势。钓鱼左臂修正反向弯肘，浮漂与涟漪移到潭心，鱼线末端同步修改；修复坐姿误叠加待机呼吸导致握绳位置漂移。希尔钓鱼与思考后续也接入完整进出场，其他角色不扩展。

- 七名 SVG 吉祥物新增“思考”：分别绘制弯肘、抬臂和托腮手形，替换对应原垂手，配合独立头部轻偏和主题色小问号渐显，八秒循环。保留各自袖口、手套、持物手及原有头部比例；053 鹿角单独收小偏头角度，避免裁切。右下角“待机/思考”按钮记忆动作，暂停与减少动态效果设置继续生效，选择预览保持原静态姿势。用户反馈后撤回试作蹦跳，旧 `hop` 偏好兼容转为思考；七张 SVG 中的专用姿势分组共用 `actions.css`，没有逐帧脚本。

- 设施功能研究详情收紧顶部布局：将“返回研究树”移到研究图标与名称同一行并靠右对齐，移除单独操作行，桌面和手机详情高度更紧凑。
- 继续收紧设施页基线：研究详情内容底部与右侧信息栏对齐，设施页桌面布局取消多出的底部内边距，避免中间区域多出约 20px 并触发页面滚动条。
- 修正伙伴邮件仍可外页滚动的实际根因：App 桌面公共选择器覆盖了页面局部高度，使整列头像撑高父层。新增路由专属 `is-mail-reader` 外壳约束，中间页与两侧栏等高，筛选后剩余空间作为邮件区域；取消手机固定最低高度，三栏独立滚动且首尾不向外传递滚轮。隐藏原生滚动条，添加随真实溢出变化的方向提示；继续保留无内嵌小边框结构。

- 伙伴邮件按游戏截图替换为原素材皮肤：多边形头像框、金色选中角框、橙色信件选中底图及带标题条的完整信纸。修正将 `mail_page` 列表按钮误平铺成信纸的问题；保留顶部角色筛选，窄屏头像改为横排，信件正文独立滚动。皮肤封装于 `PartnerMailReader.vue`，按钮复用 UiButton，正文以纯文本保留换行，发件人显示“来自：角色名”。

- 米托拉 SVG 统一改名为 `hero-002-idle.svg`，同步资源引用；围绕围巾连接处将头部（脸、刘海、耳朵和发饰）整体缩小 10%，保持身体、长发和内部动画坐标，改善七角色并排时头部偏大的观感。选择窗继续显示中文名。

- 角色营地事件按标题过滤：名称包含“测试”的条目不显示，不限定角色；主角和其他角色的正常事件保留，营地事件页签和章节标题保留。仅调整展示，不删除原表或预解析数据；规则同步 `SPEC.md`。
- 角色详情仅为主角 `hero_001` 增加男女立绘：桌面左女右男，手机默认女主，右上角“切换男主/切换女主”只切换立绘，不改资料与路由；其余角色保持单立绘。
- 右侧信息栏 SVG 吉祥物按 `public/test2/hero` 参考图手绘，完成 001 希尔、055 迦南、062 露比特·鲁特、053 艾薇杜尔、034 露帕·萝特、049 菲莉娜，保留原有 002 米托拉；默认希尔，共用呼吸、眨眼与发梢轻摆待机 CSS。修复 055 肩侧剑身断开，将剑刃接为连续形状。插画位于说明下方靠右下，不阻挡正文和装饰区域的鼠标操作。
- 吉祥物复用 `UiButton` 提供播放/暂停并记忆角色及暂停偏好；“切换”通过公共 `UiPopover` 向上展开横排角色小像窗口，使用侧栏棕色边框、装饰钉与同色不透明羊皮纸背景，避免下层文字透出。当前角色高亮，点选成功后收起；窗外、Esc、路由变化和卸载关闭，键盘展开及选择恢复焦点。SVG 首次只加载当前角色，展开后才加载静态预览，成功结果内存缓存，失败保留原角色并允许重试。
- 后台、离开可见区时暂停，系统减少动态效果时静态展示并保留选择按钮。桌面按需加载，移动端、原生端、高度低于 700px 的窗口以及招募独立布局不挂载；700~780px 高的窗口缩小插画。

- 普通兑换卡片视觉细调：名称按用户最终要求保持单行，标题和限购之间留白；限购改用弱底色、淡边框和次要文字色；价格缩小并改用主题正文色；删除图标上方分隔线，底线减淡。按原图可见包围盒微调图标等比缩放，保留原始图片。外框仍为 3:4，桌面列数和手机四列不变。
- 兑换页普通商品与商城商品共用 3:4 宽高、标题区、图标容器和价格区，修复长名称撑高普通卡片、图标偏大的问题；长名称单行省略并保留完整悬停标题。保留各自羊皮纸/商城背景，桌面最小列宽 110px、手机每行四张。
- 兔子商人隐藏重复的单项子类行，活跃兑换隐藏“s1”；种子和兔子在现有筛选末尾新增“刷新规则”页签，顶部摘要和卡片说明统一移入。复用 `UiSection`、`UiListRow` 和羊皮纸面板，按原有规则分组展示商品，保留候选池、每次购买数量及种子任务解锁条件；支持 `view=rules` 分享/刷新与切回商品筛选。
- 来源说明统一精简：银币等日常来源只写“通关结算奖励”，去掉任务全文、消耗、数量与普通奖励必得提示；礼包、钱袋和招募附赠也采用短说明。保留宝箱、特殊装备与符石鉴定概率、采集位置以及首通/研究限定产出的必要区别。塔层、分解和招募转换合并重复项，已有“前往”定位保留。

- 邮件正文复用角色图鉴已有 `cleanMailContent`，统一将主角占位名处理为“小工匠”，并清理邮筒和隐藏标记。
- 根据页面截图反馈移除伙伴邮件外层自定义阴影，信纸上下与头像栏、信件栏齐平；头像栏横向与竖向滚动条均隐藏，正文滚动仍保留。

## 数据与解析

- 邮件皮肤从 `UI_Atlases/EmailPanel_Atlas`、`Common_Atlas` 与 `4.24路资源包/assets/res/texture/uipanel/emailpanel/mail_botm.png` 原样复制 8 张 PNG 至 `public/images/EmailPanel/game-skin/`，每张 SHA-256 均与源图一致，未压缩。按图集边界与邮件 prefab 的 UITexture 四边 150px 配置进行 CSS 切片；数据仍复用已有角色档案邮件，不虚构账号时间、领取或完成状态。
- 修改前页面与邮件素材保存在项目外 `vue-myrzg备份-资源/partner-mail-before-game-skin-2026-09-10/`；现有资源未覆盖，恢复备份页面即可回退旧样式。

- 男主立绘从资源包 `chara001b_0.png` 原图获取，复用 `水印添加.py` 与项目压缩工具处理。按用户反馈扩大水印比例框，保持中心与透明度，重新从原图生成；最终 440×1003 透明 PNG 为 111,104 字节。原图、水印未压缩版与旧处理结果均保存在同级资源备份目录；女主及已有旧男主资源不修改。处理脚本与参数见 `PROTAGONIST_PORTRAIT.md`。
- 按角色图鉴 `hide !== true` 导出 34 名正式角色的正面待机透明 PNG 到 `public/test2/hero`，共 12,996,292 字节（12.39 MiB）；附 `hero-models.json` 中文名、模型、图片及哈希清单。导出脚本增加 `--all-heroes`，不读取 NPC/怪物表，不包含额外时装。拉碧丝的裁切 Sprite 在内存补回画布位置，修复部件错位，原始图片未修改；批量命令和修正规则同步导出说明。

- 小人导出工具增加 `--hero` 与 `--output-dir`，支持按角色原表导出普通模型，不更新皮肤清单；已实际导出米托拉 `Npc_002_1` 的正面待机透明图到 `public/test2/new_hero_002.png`（396 KB）并检查图片，使用方式同步 `SKIN_MODEL_EXPORT.md`。

- 来源精简从生成器落实，去掉副本重复名称、外层物品 ID、未使用概率数值等字段，以及日常前置条件、奖励 ID 和招募时间的冗余副本。`item-sources.json` 从 3,003,156 字节降至 1,915,462 字节（2.86 → 1.83 MiB，减少 36.2%）；本地 gzip 测算减少 32.7%。来源记录 11,240 → 11,212，合并塔层 2 条、分解 20 条、招募转换 6 条重复项；507 件可见物品的结构化来源覆盖、8,040 条副本位置关系均保留。

- 页面消费 `public/data/parsed/heroes.json` 中由 `heroMail.json` 与角色档案关联生成的邮件数据，未修改原始表。

## 验证

- 希尔钓鱼与思考完整流程最终在 4187 回归：`hil-actions.spec.js`、`hil-sequence.spec.js`、`sidebar-mascot.spec.js` 共 20 项通过、20 项按设备条件跳过。逐段检查钓鱼出现/转身/坐下/伸手/取竿/放线/回稳/收线/放竿/松手/起身/收道具，以及思考抬臂/托腮/偏头/收手/回正；取放竿在 25%、50%、75% 处握点误差小于 1px，放收线与咬钩中途切走的竿尖/鱼线/浮漂连接误差小于 0.6px。新增手指、视线与暂停断言、拿竿中途暂停后连续改选断言均通过，原秋千、七角色选择与响应式回归通过。已查看关键阶段截图；Vite 生产构建与相关差异空白检查通过。此次未重建游戏数据、运行全量 verify、做真机性能测试或发布。

- 希尔秋千流程、弯肘与鱼漂居中修正完成最终回归：`hil-actions.spec.js`、`hil-sequence.spec.js`、`sidebar-mascot.spec.js` 在 4187 共 18 项通过，另 18 项按设备条件跳过。覆盖完整进出场各分段、同一个人物节点持续可见且无裁切、中途暂停/恢复、后台冻结、连续点选保留最后目标、减少动态效果中断、卸载取消、握绳/鱼线连接、左右手与四动作比例、浮漂入水点与潭心重合、左肘低于肩部及七角色选择回归。已检查出现/靠近/握绳/坐下/松手/跳下/落地和修正后钓鱼截图；Vite 构建与相关差异空白检查通过。未执行游戏数据重建、全量 verify、真机性能实测或发布；完整进出场仅先接希尔秋千。

- 希尔共用手臂、统一比例及水潭调整在 4187 完成 `hil-actions.spec.js` 与 `sidebar-mascot.spec.js` 回归：15 项通过、15 项按设备条件跳过。新增检查确认四动作人物/手套缩放一致、左右手套大小一致、切动作不移动说明和侧栏、900px 与 700px 高窗口都有大于 14px 的脚边干岸且浮漂落在水中；原有抬手、握绳、鱼线跟随、七角色选择、暂停与减少动态效果验证均通过。已查看四姿势、托腮中段及矮窗口截图；Vite 生产构建与相关差异空白检查通过。此次未重建游戏数据、运行全量 verify、做真机性能测试或发布。

- 希尔新动作与原角色专项首轮 13 项通过、1 项新增前臂暂停失败（另 14 项设备条件跳过）；修正动画样式优先级后希尔 3 项复验全通过。验证手掌抬起超过 40 个屏幕像素且回到原位、问号与手势同步、暂停冻结前臂、秋千握点与绳索误差小于 1px、竿尖和鱼线/浮漂连接误差小于 1px、场景刷新记忆、减少动态效果及其他角色不出现专属选项。已检查思考/摆动/提竿各阶段截图与矮窗口效果，最终 Vite 构建通过，未修改游戏数据或发布线上版本。

- 七角色思考动作在 4187 完成最终专项回归 11/11 通过（另 11 项按设备条件跳过），覆盖专用手臂互斥、偏头四阶段无裁切、预览静止、问号与头部暂停、动作/角色记忆、减少动态效果、矮窗口控制行及原有选择交互。逐一检查七角色思考截图；最终 Vite 构建通过。此次未修改游戏数据或发布线上版本。

- 设施研究详情桌面/手机专项回归 10/10 通过，覆盖研究详情、前置跳转、返回研究树、树布局与移动端横向滚动；`npx vite build --logLevel error` 和差异空白检查通过，验证端口为 4187。调整后构建再次通过；首次并行构建因已有 dist/images 占用而失败，未作为最终结果。
- 邮件固定区域专项在 4187 完成 4/4：实际内容筛选与切换，以及 35 封邮件/100 行长信夹具验证。覆盖桌面 1912×914、1440×900、1280×720，手机 390×844、844×390；测量外页 scrollHeight 不超过 clientHeight、邮件底部与侧栏误差小于 2px，实际滚轮在三栏及末端操作时外页 scrollTop 保持 0，返回普通图鉴释放锁定。已查看桌面与手机横屏截图，Vite 生产构建通过；本次未变更数据或图片、未做全站回归及真机验证。

- 邮件原素材皮肤在 4187 完成桌面/手机专项 2/2 通过，覆盖图片、角色筛选、信件切换、空状态和正文宽度；实际查看两端与暗色截图。`node node_modules/vite/bin/vite.js build --logLevel error` 通过，8 张原图哈希核对一致。本次没有重建游戏数据、全站回归、Android 真机验证或发布。

- 米托拉改名与头部比例调整在 4187 完成角色切换/选择窗专项 2/2 通过，已检查单人和并排截图；最终 Vite 构建及相关差异空白检查通过，旧素材引用已清除，生产资产生成 `hero-002-idle` 哈希文件。

- 营地事件标题过滤在 4187 端口完成桌面/手机回归 4/4：实际主角测试事件隐藏、其他角色正常事件保留，并用浏览器数据夹具验证主角正常事件保留、非主角测试标题隐藏。`npx vite build --logLevel error` 和相关差异空白检查通过；未重建原始数据或发布。
- 主角展示及原有皮肤面板桌面/手机回归 6/6 通过；放大水印后主角专项复验 2/2 通过，检查桌面并排和手机男主截图。最终 Vite 生产构建、差异空白检查通过，原图备份 SHA-256 与资源包一致；开发验证端口为 4187。
- 34 张角色小人图导出成功，逐项核对角色名、骨骼、皮肤、文件哈希、非空像素及透明边缘；已检查全角色总览和修正后的拉碧丝单图。导出脚本语法与差异空白检查通过；此次只生成指定目录素材，未接入页面或重建发布产物。

- 吉祥物在指定 4187 端口验证共 10 项有效场景：首轮 9 项通过、1 项展开焦点失败（另 10 项按桌面/手机条件跳过），修复焦点后相关 3 项复验通过，最终不透明底色调整后窗口专项 1 项通过。覆盖明暗主题、上方定位、七角色点选、静态预览、窗外/Esc/路由关闭、焦点恢复、刷新记忆、加载失败重试、鼠标穿透、暂停与响应式。已检查明暗和矮窗口截图；页面后台用可见性事件模拟，不宣称真机耗电实测。
- 六张新增 SVG 每张约 7–8 KB，原有米托拉约 11 KB；没有新增动画库、逐帧 JavaScript 或参考 PNG 下载。最终 `node node_modules/vite/bin/vite.js build --logLevel error` 通过，七张 SVG 均生成带哈希的生产资产。本次仅 UI/插画改动，未重新生成游戏数据、运行全量 verify 或发布线上版本。相关差异检查仅报告 `App.vue` 已有尾部空行，未修改其他任务的格式。

- 上述视觉细调在用户指定的 4187 开发端口验证：最终单行名称版本桌面/手机共 8 项回归通过，覆盖等高卡片、标题/图标/价格无重叠、图片加载、规则页、详情返回和时装回归；已检查最终浅色/深色截图。`npx vite build --logLevel error` 与本次差异空白检查通过。
- 兑换尺寸、刷新规则、商城和时装桌面/手机回归 8/8 通过；最终规则面板调整后，尺寸与规则专项复验 4/4 通过。覆盖卡片/图标/价格尺寸一致、长标题、四列手机布局、图片加载、详情返回、隐藏子类、规则直达及重载；已检查两端卡片与规则页的浅色/深色截图。最终 `npx vite build --logLevel error` 和差异空白检查通过。
- 用户确认后续默认需要验证，仅当次明确要求“不验证”时跳过，不将之前的跳过要求延续到其他修改。
- 来源精简专项单元测试 30/30、桌面/手机页面回归 10/10 通过，检查了银币两端截图；容器反查和副本普通奖励缺口仍为 0。完整 `npm run verify` 首次因沙箱清理 dist/data 权限失败，获准重试后通过；dist 来源数据与 public 逐字节一致。本轮未做弱网性能实测或 Android 真机验证。


## 未完成事项

- 系统邮件和真实账号已读/奖励状态未纳入当前静态资料库页面。

## 文档同步

- 邮件页面、组件皮肤例外及数据职责同步 `SPEC.md`、`ARCHITECTURE.md`、`UI_COMPONENT_LIBRARY.md`；资源来源、切片证据和恢复位置记录在 `features/PARTNER_MAIL_SKIN.md`。

- 整理 `docs/` 文档目录：专题功能移入 `features/`，技术说明移入 `technical/`，来源审计与阶段交接移入 `audits/`；新增文档导航，开发日志继续按日期保留在 `dev-logs/`。
- 吉祥物的装配职责、素材位置、视口边界与动画规则已同步 `SPEC.md`、`ARCHITECTURE.md` 和 `UI_COMPONENT_LIBRARY.md`；参考图和游戏原表均未改动。

- 兑换卡片统一尺寸与刷新规则页签约定已同步 `SPEC.md`、`UI_COMPONENT_LIBRARY.md`；此次不修改原始配置或预解析数据。
- 来源精简规则、字段裁剪、重复项合并与体积对比已同步 `SOURCE_COMPLETION_FOLLOWUP.md`、`SPEC.md`、`ARCHITECTURE.md`；此前补全统计在文档中明确为历史阶段，待核查 79 件清单保持不变。

- 已记录本次页面、数据来源和验证结果；总规范与架构未发生结构性变化。

- 伙伴邮件页补齐游戏彩色邮件类型图标：按 `EmailPanelUI.cs` / `EmailItemTemp.cs` 的普通、奖励、任务及任务 PT 分支选择 `mail_list_new*` 素材；邮件三栏增加单层符石图鉴风格边框与纸张背景，并将中栏标题内容右移避开装饰分隔线。4187 端口 Playwright 4/4 通过，Vite 构建通过。
