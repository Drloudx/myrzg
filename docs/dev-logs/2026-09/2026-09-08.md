# 2026-09-08 开发汇总

## UI 与交互

- 新增家具图鉴导航、搜索与分类/品质/放置区域/图纸筛选，详情展示制作材料、外观及家具图纸；家具与物品详情支持按 URL 参数双向跳转。
- 家具采用独立的 `FurnitureCard` 和虚拟网格。卡片及详情只展示 `BuildItem_Atlas` UI 图，缺失图标统一占位，不使用基础家具图或房间场景立绘代替具体外观。
- 家具详情首屏改为与角色详情一致的居中大图展示，并在整个展示区恢复随家具品质变化的浅色背景；目录调整为桌面 5 列、手机 3 列，手机卡片同步收紧次要信息。来源标记保留原始 `sourceTags`，并通过全局 `getMapName()` 把 `c0~c5` 转为地图名称后展示；图纸行“查看物品”补足右侧留白。
- 奖励、掉落、剧情奖励与家具制作材料统一复用 `UiRewardCard`：整卡固定羊皮纸底和中性描边，图标槽采用图 4 的 44px 品质底色，不叠加额外槽位阴影或边框；任务、事件和成就构建数据同时补齐原始 `item.quality`。

## 数据与解析

- 新增 `furnitureData` 与 `parsed/furniture.json` 构建链，联结 `homeItem/item/gameSetting/consume/playerInit/condition/task`。当前 142 件是静态配置经本站规则筛出的图鉴全集，不等同账号已解锁家具；源码显式排除 `cultivation001`，另按表内“废稿”语义排除 13 条配置。
- 物品图鉴收集分类纳入有静态图鉴家具关联的 `unlockHomeItem/unlockHomeItemSkin` 图纸。正反向关系只按 `useActionPara.homeItems[].typeId/skin[]` 建立，`objType` 图鉴分类与 `category → sourceTags` 来源标签分离；普通图纸和皮肤图纸分别依源码使用家具基础 `icon` 与首个指定皮肤 `icon`。
- 开放条件按源码 `CheckResultConditions` 解析 `rules/need/reverse`，并联结任务表显示任务名与完成步骤；不再把 `condition.desc` 配置备注当真实条件。
- 从 `UI_Atlases/BuildItem_Atlas/sprites` 原样复制当前静态图鉴配置引用的 143 张 UI 图。4 个原表引用没有对应 UI 图：`build_roomLittle_yma7_2`、`build_roomLittle_yma8_2`、`build_roomLittle_yma8_3`、`build_roomOther_xca4_1`，保留为明确缺图状态。

## 验证

- 家具数据契约单测 6/6 通过；全量 Node 单测 51/51 通过，其中新增 3 条奖励品质数据契约测试。家具 Playwright 专项在桌面与手机两套视口共 8/8 通过，覆盖筛选计数、家具/物品图纸双向跳转、缺图占位、源码开放条件和普通图纸/家具预览的图标差异。全站 UI 并发回归因本机 8 浏览器争用出现 11 条超时，全部以单工作进程复跑通过。
- 完成 [2026-09-07 技术治理](./2026-09-07.md) 的跨日收口。生产预览的物品、任务、角色与角色剧情均有实际内容，无 pageerror；items/tasks/heroes 和角色剧情分片请求携带 64 位内容哈希，JSON 与对应清单均为 HTTP 200。
- 最终生产构建通过；dist 只包含两份 HarmonyOS 字体，副本移动末尾留白已进入生产 CSS。
- 本地开发预览保留在 `http://127.0.0.1:5175/`。5173 属于既有服务，本次未停止它；新服务使用独立 Vite 缓存。生产测试用临时 5176 服务已停止。

## 文档同步

- 汇总上一日实施记录，补充深木/封面前景 Token 约束；没有新建按单次修改命名的日志。

## 未完成事项

- Android 真机更新/返回验收及外部无效原表核查仍按上一日日报记录，不属于本次已验证结果。
