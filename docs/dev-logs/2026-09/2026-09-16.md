# 2026-09-16 开发汇总

## 重构与维护

- 二次恢复 SPEC：此前一次整理把主规范压到约 18KB，删掉了接手项目所需的页面规则和数据链。本次恢复为约 56KB：补回应用壳、共享模块、组件边界、长列表、状态、构建产物、热更新、来源真实性以及各页面的中等详细契约，同时继续删除逐像素调参、重复 API 表和历史施工过程。
- `features` 六篇保留各功能独有约定；设施/符石合并重复筛选布局，删除历史概率数字快照与测试计数，招募去掉易失效的逐面板缩放状态。`technical` 两篇保留跨页奖励结构/概率边界和仍在使用的模型导出命令，去掉与 UI/符石专题重复的接入示例。
- 公共组件接口与皮肤例外集中到 UI 文档，压缩兑换卡片的调参说明；架构只描述职责和机制，发布步骤归架构、资源授权和备份归 SPEC。文档导航说明两个专题目录的具体用途。
- 核对代码修正物品/角色/事件/成就运行时数据、物品详情签名、热更新版本时机、招募记录 URL、成就与隐藏物品的双收集状态及其他奖励页筛选参数；纠正普通桌面列表/邮件阅读器/地图滚轮职责混用。
- 保留邮件 844×390 长正文历史失败的未复测说明；将招募旧审计移出功能目录，旧说明保存在同一历史摘录，不新增重复的现行专题或多份日报。

## 验证

角色属性标签修正：`unitData.atkSpeed` 显示为“基础攻速”，并新增战斗公式专题，按源码整理属性成长、攻速/攻击间隔、冷却缩减、原始伤害、防御穿透、暴击、等级压制、抗性、治疗、吸血和反伤公式。公式说明区分了通用结算与技能/Buff 专属触发链，未把未证实规则写成硬公式。

角色图鉴技能标题补回 CD 与消耗：原先读取技能顶层，实际字段位于对应等级的 `levelData`，现与当前等级说明使用同一数据项；标签紧邻技能名称。原表与预解析产物核对“免费赠饮”为 CD 22 秒、消耗 44，“强买强卖”为 CD 16 秒、消耗 32。角色技能/升级材料桌面与手机回归 2 项通过，未重建数据。

后续横屏截图复核：卡池首页也改为宽屏延展布景，裁去模糊纹理透明边，立绘与控件保持原比例。修复 WebGL viewport 未随缓冲区同步导致的翻卡角色偏左、桌面右端断口；补充两类卡池背景覆盖和 GL 实际绘图区断言。相关桌面/Pixel 5 适配回归共 16 项通过，并检查超宽横屏截图。前轮 `npm run verify` 曾启动，但未取得完整退出结果，不计为验收通过；本轮未重跑全量构建。

本轮招募移动端修复：新增 `GachaViewport` 处理触摸设备竖屏横置；按源图有效区域裁切翻卡背景；修正揭晓 Spine 小人的 DPR 缓冲区和台座脚点计算。`tests/ui/gacha-mobile-layout.spec.js` 与 `tests/ui/gacha-responsive.spec.js` 在桌面/Pixel 5 共 14 项通过，覆盖旋转、弹层、超宽背景、途中改比例、波特温/米拉贝尔和两类十连结算。未运行完整生产构建；真实微信机型仍需人工复核安全区差异。

- Markdown 本地文件与章节链接检查通过：覆盖项目 README 和 docs 内 24 个 Markdown、98 个链接，排除代码块中的历史示例。
- 文本编码只读检查：1916 个文本文件通过。
- `git diff --check` 通过。历史快照与本轮读取的原文逐段比较一致。
- 前面的文档整理阶段未运行构建、`npm run verify` 或 UI 测试；后续招募修复的验证单独列于本节，不据此声明其他历史功能问题已修复。

## 文档同步

### 战斗源码机制复核

- 按 `CombatPanel.CalculateMaster/CalculateFinalDamage`、`UnitAttribute`、`HeroBattleAttributeGroup`、`BuffBase/BuffControl` 和英雄/PvP/怪物/宠物初始化链重新核对战斗机制，扩充 [战斗机制与公式](../../technical/COMBAT_FORMULAS.md)。
- 已确认 `monLevelStrength` 的物理/魔法 K 系数、等级压制参数、四档角色基础攻击浮动、特殊 200% 暴伤、两组不同抗性区、两次伤害取整、护盾/吸血/反伤顺序、Buff 概率与叠层边界，以及房间胜利恢复和持续回复的区别。
- 明确标记英雄穿透转入、特殊属性列表加入、`damAtkAddTypes`/`addSp` 消费和霸体过滤等尚未闭合的源码链，已确认结论保留源码依据。
- 本轮检查：四份相关文档的 70 个本地文件链接可解析；K 系数、16 级压制阈值与 PvP 生命倍率通过原表断言；1920 个文本文件编码检查通过，`git diff --check` 通过。仅更新文档，未运行网页构建或 Unity 战斗，未提交/推送；现有工作区其他改动保留。

已同步 SPEC、架构、UI、排障、导航、项目 README 和相关专题；修正改名后的章节引用及日报里的错误相对路径。旧日期开发日志保留当时记录，不改写为现行结论。

## 发布与回退

无需发布、同步 CDN 或构建 APK。旧招募审计在 `backups/audits-archive/GACHA_REPLICA_AUDIT_2026-09-13.md`；[整理前摘录](../../../backups/audits-archive/DOC_DETAILS_2026-09-16.md) 保留移出内容与本轮 SPEC/专题原文，可按需恢复对应段落。

招募修复前按用户要求将已有工作区建立为本地回退提交 `9530a29`（含此前文档、代码、测试及视频），未推送；本轮修复保留为未提交改动。未改动原始图片与配置产物。

## 未完成事项

文档整理及本轮招募移动端修复已完成；尚未使用真实微信机型验收，其他历史运行时限制仍以各专题和相应日期日志为准。
