# 2026-09-17 开发汇总

## UI 与交互

- `/rewards` 原“占位奖励3”替换为“战斗规则”，入口支持 `tab=combat_rules`，兼容旧 `tab=ph3`。
- 战斗规则改为完整中文公式阅读页：上限开篇，随后逐项展开养成面板、战斗属性、攻速/冷却、伤害链、防御、暴击、抗性、等级差、护盾/回复、状态、怪物、宠物和挑战赛。取消摘要卡片与反复举例的写法，公式逐行展示，字段译为属性名称；宠物七项公式保留成长值、当前等级、系数及例外说明。数值截断统一写“取整”，说明去掉小数而非四舍五入。
- 规则页复用赛事规则的纸面布局，支持搜索、刷新恢复和手机窄屏布局；暗色底板与文字对比已复核。

## 文档同步

- 删除战斗公式专题中“和帖子问题对应的结论”章节及相关帖子措辞，保留源码、配置和待确认项作为依据。
- `SPEC.md`、`docs/README.md` 更新战斗规则入口和文档职责；完整技术细节仍在 `technical/COMBAT_FORMULAS.md`。

## 验证

- `combat-rules.spec.js` 桌面/手机通过：上限首屏、规则搜索、刷新恢复、暗色模式、旧占位链接、奖励数据失败时的独立显示。
- 原有奖励页地址恢复和搜索测试通过。最终专项运行共 6 项通过、2 项按原测试的桌面限定跳过，退出码 0。
- 1924 个文本文件编码检查及 `git diff --check` 通过；未运行完整生产构建，未提交或推送。

## 角色成长计算器

- 根据 `HeroBattleAttributeGroup`、`HeroMsg`、`HeroStarUI` 和 `general/heroRank/heroStarLevel` 配置核对角色成长：等级每级 +5%，星阶技能已升级等级总和每次 +1%，每次完成等级突破按对应 `attUp` 累加（当前配置每次 +15%）。升星次数不是角色稀有度。
- 角色图鉴基础属性页新增升星次数模拟、突破完成状态和完整代入公式；五项成长属性、突破费用和材料使用同一突破状态，基础攻速等静态属性不套成长公式。
- 桌面/手机 `hero-growth.spec.js`、`hero-material-links.spec.js`、`combat-rules.spec.js` 共 8 项通过；数据预解析和 Vite 生产构建通过，未提交或推送。

## 战斗规则全文措辞复核

- 统一“星阶技能”、资源和百分比代入方式，用中文解释舍入、第一组增减伤、内部冷却、怪物倍率和挑战赛生命倍率，保留全部章节与公式。
- 补充平均暴击概率范围、强制暴击前提及新旧穿透封顶；区分无时限状态与时长被缩短到零，明确通用反伤是一次状态效果伤害、概率检查通过不等于最终触发。
- 对照通用伤害顺序，补上动态增伤之前的攻击触发效果；页面不再使用源码来源声明，技术依据仍留在专题文档。
- 本轮规则数据结构检查通过（20 章、93 条），桌面/手机战斗规则现有测试共 4 项通过，覆盖搜索、刷新、暗色模式及窄屏布局；语法和差异空白检查通过。

## 页面搜索筛选折叠

- 18 处页面内搜索区接入公共 `UiFilterPanel`，`UiSearchInput` 右侧提供羊皮纸底、棕色文字的“收起／筛选”按钮；桌面与手机默认展开。使用 `v-show` 隐藏筛选区，已选条件、搜索、清空与路由保持原行为，折叠状态不写入持久化存储。
- 魔物收益列名/排序表头放入常驻 footer；顶部全局搜索无对应筛选区，不提供空开关。组件支持键盘操作、可访问状态、触屏 44px 点击高度和明暗主题。
- 新增折叠专项验证：各页收起/展开、筛选保留、搜索与清空、暗色布局、手机列表可视高度及邮件横屏折叠后的正文可见性，桌面/手机共 40 项通过；生产构建通过。
- 营地入口复查：当前 5173 开发页面的“营地升级”“属性研究”均显示搜索右侧折叠按钮。补充建筑直达链接及三个页签反复切换的回归，桌面/手机 6 项通过，覆盖折叠、恢复与路由保留。未复现用户端按钮缺失，尚不能确定其原因；本次没有更改页面实现。
- 营地正文底边对齐：移除研究详情固定 `100dvh - 307px` 最小高度，桌面营地纸面通过 flex 填满筛选栏下方剩余空间；短内容展开/收起均与两栏齐底，长内容仍随页面滚动。按反馈图 1912×914 实测两种状态底边均为 894px。营地及折叠回归 17 项通过、1 项手机无侧栏跳过。
- 扩展回归（应用壳、虚拟列表、伙伴邮件、符石、营地）85 项通过、34 项按既有条件跳过、3 项既有问题失败。**2026-09-18 复核更正：本条的失败归因已失效，勿再据此判断实现问题。** 实测（1440/1280 视口 + 产物取值）确认：兑换栅格为 `auto-fill minmax(110px,1fr)` 自适应列数（1440 宽 6 列、1280 宽 5 列），`exchange-card-sizing.spec.js` 早已按 `desktop ? 6 : 4` 断言，并非「仍要求桌面四列」；`商店积分` 子类含正式子类「时装」（`packDisplay.fuZhuang` / `packType: 5`，2 条目），`exchange-skins.spec.js` 桌面与手机均通过，不存在「旧分类列表未包含时装」；伙伴邮件 844×390 展开筛选时正文高度为 140px（收起 229px），并非为零。当时的 3 项失败实为 `app-shell.spec.js` 两条过时断言与 `exchange-card-sizing.spec.js` 默认 45s 超时预算不足，均已于 2026-09-18 修正。
