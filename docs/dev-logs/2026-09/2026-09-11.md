# 2026-09-11 开发汇总

## UI 与交互

- 用户反馈后改为逐角色精修；本轮先处理 034 露帕·萝特，重新调整脸型、眼睛和紫色刘海曲线，使其与原图的蝴蝶结、长发、法杖和服装形成独立形象。仅修改 `hero-034-idle.svg`，其他角色不受影响。
- 修正 034 法杖在站立画面不可见的问题：保留法杖原有独立分件，恢复到身体后层并调整站立/坐姿偏移，避免遮脸或越界；待机和钓鱼画面已重新核对。
- 按用户要求调整 034 法杖层级：待机/思考时显示在右手旁并保持握点可见，秋千/钓鱼时回到身体后层；复用同一法杖节点和动作轨迹。
- 修复 034 法杖杖头与杖身的断层，增加连续的杖颈和高光连接段；已重新检查待机画面，法杖成为完整连续几何。
- 根据复查继续修正 034 法杖：增加贯穿杖头至底部的直线结构杆，并去掉待机旋转偏移，避免手部位置看起来弯折或断开。
- 034 法杖改为统一斜背姿态：四个动作共用单一法杖几何，移除重复叠加杆段，使用固定缩放和旋转保持杖头、杖身连续；实际页面待机/钓鱼画面已复核且无越界。
- 根据用户要求，以首批七名的细描边、平涂和分区阴影重绘后续全部 27 名吉祥物，替换此前轮廓描摹版本。当前 34 名均为真实分件关节模型，支持待机、思考、秋千、钓鱼及完整进出场；后续四批分别为 005/007/008/009/011/012/014、015/019/023/025/026/027/029、031/033/036/037/041/043/045、046/050/051/064/065/066。保留每批七人的上一批/下一批切换，跨角色保留动作。
- 逐个核对原形象的肤色、发型、耳角、武器、背包、围巾、甲片与饰物；大锤、镰刀、钟表杖、双剑、矿镐等拆为完整随身部件，坐下时收拢。茜塔不额外添加武器；狼/虎/狐角色保留口鼻和爪脚；异色袜靴、女仆袖口和绷带分别适配。后续角色身高按待机第 0 帧实测归一，动作中不改变人物比例。
- 按用户要求对后续 27 名继续逐图精修：圆弧发束、翼膜/火焰分区、甲片明暗、袖褶、腕扣、膝甲及异侧绑带。重画 023 锤头厚度面、033 镰刀钩头、036 钟表杖、066 红柄斧；补齐 019 魔法书与火焰随行生物、027 书袋流苏、046 腰链和手铐。045 双白头羽、026 大耳、051 曲角及 065 额卷重新调整轮廓；037 肩胸、上臂和毛皮护腕加厚。完整逐角色记录与原图/四动作对照见 `audits/MASCOT_FIDELITY_2026-09-11.md`。

- 邮件正文下滚箭头从阅读器右下角移到正文底部居中、奖励栏上方。通过独立 `.mail-body-area` 定位，保持原正文和奖励位置，有无奖励均适配；滚到底后隐藏。

- 邮件列表统一卡片高度（桌面 66px、手机 56px），标题从 13px 按 1px 逐级减小到单行可容纳，窗口变宽和字体加载后重新测量。手机 ≤700px 改为头像横栏、横滑选信栏、全宽正文，右侧前后按钮及封数辅助切换；单封不显示切换按钮，首尾禁用。横滑停稳后自动选中最近信件，正文与选中标题同步。

- 伙伴邮箱从仅档案关联的 37 封扩展为 36 位发件人、61 封邮件，补齐主线/支线后续来信以及格薇勒、阿迪拉、艾茵的邮箱。标题搜索覆盖全部邮件；普通信、附件、普通任务邀请、档案任务沿游戏字段使用彩色图标。两封带 `img=01` 的信展示原附图，奖励数量使用实际 `min/max`，修正旧解析遗漏“×3”等物品数量。

- 邮件奖励标签按用户指定改为 30×60px，奖励格同比例调整为 60×60px；奖励区保留 64px flex 基准，边距改为 `0 6px 24px` 并去掉顶线。数量按 `Const.cs` / `ItemBagCellUI.SetCnt` 使用 `ColorString[1]=#F8EEDC`，网页局部字号为 13px；现有深色阴影尚未对齐原控件。
- 纠正此前把奖励数量颜色套用到标题的错误：按 `EmailPanelUI` / `EmailItemTemp` 控件 PathID 解码原始 `emailpanel.asset`，两处标题均为 `#CFBA96`、alpha=1，无渐变和文字特效；已修改两处 CSS。左/右标题游戏字号分别为 22/24，字体为 `HeiTi → MYR2SansRegular`。
- 原正文控件字体为 `CuHeiTi → MYR2SansBold`，字号 20、额外行距 8；普通字色由运行时 `ReplaceDescValue(...,10,15)` 设置为 `#533E26`。发件人为同一粗字体、字号 18、`#533E26` 且 alpha≈0.698。已定位实际字体资源，撤回此前“没有字体资源”的记录；按用户最终指定，网页使用现有 HarmonyOS SC 常规/粗体，移除本轮误引入的两份 MYR2Sans 副本，源资源保留。
- 邮件标题和副文字使用常规字重，正文/发件人用粗体；发件人恢复原色及透明度，列表副文字采用原 timeLabel 的 `#F8EEDC`、alpha≈0.502。标题与发件人增加 8px 间距，信纸顶部切片同步加高 12px，正文行高按游戏比例设为 1.4。奖励数量从右侧 4px 改为 7px，向左收进 3px。

- 米托拉（002）补齐共享关节版思考、秋千、钓鱼及完整进出场：斜托下巴、偏头与眼神/手指变化；握绳坐下、轻荡和跳下；出凳子、侧身坐下、拿竿放线、看漂提竿、收竿起身。保留红白长发、披肩和蓝色吊坠，适配交叉绑带袖口、左露指手套与不对称袜靴；长发坐下收拢、起身连续恢复，头部保留原 .9 比例。按新关节轮廓将原始待机身高校准为 308.745，继续统一到 312 模型单位，动作间不改变比例。
- 七人显示大小按待机可见身高统一到 312 模型单位（桌面约 187.2px、矮窗口约 140.4px），消除原约 6% 的身高差。`standingHeight` 配置和 `getMascotScale` 共用固定比例，围绕落脚点等比例缩放，预览同步；保留角色宽窄与造型，动作中不重新缩放，关节人物和道具始终一起变化。
- 七名吉祥物主画布统一为 320×400，桌面 192px、矮窗口 144px，选择窗也统一 4:5 画布；待机与思考按身体落脚点 (162,369) 对齐，避免原两套画布右对齐导致切角色左右跳。非关节素材按 `MASCOTS.groundX` 平移装配，保留局部坐标与动画。七人思考问号统一右上方、尾巴朝左下，菲莉娜也改回右侧。
- 菲莉娜原 SVG 的身体与阴影左移 40 单位，尾羽向下收拢以完整保留轮廓；旋转固化到尾羽路径，避免嵌套旋转包围框越界。装配时的身体锚点计入素材平移，选择小像与主插画同步居中，人物和手掌未单独缩放。
- 在 [前一日吉祥物工作](2026-09-10.md) 基础上，055 迦南接入与 001 希尔相同的思考、秋千、钓鱼及完整进出场流程；选择窗同步本人静态模型，换角色取消旧片段并保留支持的动作。后续角色按批次加入，当前仅提供待机。
- 迦南保留裸手、褶边袖口、护腿、长靴、连续佩剑与披风；坐下时佩剑略贴背、披风收短，站起恢复。关节角色共享画布、道具与动作流程，肩点、臂长、服装和随身物件由角色配置适配。
- 根据用户截图修正两人的手臂：握绳与握竿双肘下沉，手腕顺着前臂；钓鱼加长握柄，右手握前段、左手靠近卷线轮。思考改为肩肘腕协调抬手的独立双臂姿势，另一只手自然垂落，保留偏头、手指轻动与视线变化。
- 根据新旧对比图恢复思考的斜托下巴轮廓：两人托腮肘点向外、前臂斜抬，避免竖直拳头顶脸。思考独立分配折叠袖管的上下段投影长度，总臂长与手掌大小保持不变；`armSleevePaths` 统一静态轮廓和过渡路径，抬手/收手同步改变袖管、肘点与腕点。秋千和钓鱼的比例、握点不变；思考抬手阶段的佩剑/披风保持站立状态。

## Bug 修复

- 成品复核发现 027 书袋、046 腰链和 026 花瓣盾虽存在于 SVG，却被身体或手臂遮住；按实际穿戴关系改为腰侧/盾牌层，同一节点保留坐姿轨迹。045 头羽被刘海覆盖，重画并移至头部前层；043 前侧长发被衣服盖住，增加随头部的前侧发束；019 魔法书移到可见腰侧。最后按新轮廓重新校准 008/023/026/037/045/051/066 的固定显示比例。
- 后续角色重绘复核修正眼睛分组提取时的遮挡、025 多声明不存在的发组、036 钟表杖两段负坐标写法；统一尺寸后收拢 012 围巾尾端，避免钓鱼时左缘越界。037 红腰布移到裤装前层，033 双马尾轮廓及发丝改为连续圆润曲线。

- 艾茵来信残留 `[callName4]`：邮件清洗遗漏通用称呼映射，原通用映射替换又只识别花括号。`cleanMailContent` 改为复用 `cleanDialogueLine`，后者统一识别 `{callName1..4}` / `[callName1..4]` 并使用现有 `CALL_NAME_REPLACE`；角色档案内嵌信件同步受益，保留换行与邮筒清洗。
- 邮件奖励原先只展示卡片；预解析现保留共享奖励规则的真实 `typeId`，点击/Enter/空格通过当前路由追加 `itemId` 打开全局详情，银币等货币也使用真实物品 ID。未复制物品详情组件，保留原邮箱选择与滚动。

- 邮箱曾直接消费可玩角色 `archives[].mail`，同时遗漏档案之外来信与图鉴 `hide` 发件人；改为遍历完整原表后生成独立 `mailboxes`。原表 77 条中有 16 条档案关联已失效，按 `HeroMailServerData.GetMail` / `EmailPanelUI.InitHeroMail` 同步过滤，原表未删除。`condition` 触发的剧情后续来信不能据此误标任务邀请，图标根据原邮件 `mailType/taskTypeId/reward` 判断。

- 邮件信纸顶部切片加高后，右标题仍停留在旧位置，造成文字偏上；按原黑条中心计算后仅将标题下移 3px，发件人与正文位置保留。

- `theme.css` 字体 URL 缺少实际文件名中的 `_SC`，导致字体请求失败并回落系统字体；已改用现存 SC 常规/粗体文件，移除无使用方的 `HarmonyOS_Regular` / `HarmonyOS_Bold` 重复字体族。JetBrains 模块将 `public` 标为资源根，使 `/fonts/` 等根路径按 Vite 的 public 目录解析；编辑器诊断是否即时刷新未通过 IDE 验证。

- 用户视频显示秋千切钓鱼时，鱼竿先横悬胸前，到拿竿片段才跳到停放位置；完整刷新后的当前页面未复现该跳位，未将热更新或缓存认定为已证实原因。将初始停放矩阵与旋转中心直接写入鱼竿 SVG 属性，取消父层变量/停放 CSS 依赖，与取放片段共用同一矩阵；保留上轮米托拉右臂与停放位置调整。
- 米托拉拿竿初段右肘折向胸口：鱼竿原停放握点过于靠近肩点正下方，共用 IK 分支使前臂横穿胸前。通过模型配置将停放鱼竿外移，取竿姿势、拿竿与放竿轨迹共用同一握点计算，右肘全程保持肩点外侧偏下；手掌、臂长、身高和最终握竿姿势不变，希尔/迦南保持原停放位置。静态道具与过渡共用停放变换，避免拿起或放下时跳位。
- 伙伴邮件原先用 `heroArchives.type` 判断图标和奖励标签，遗漏了原 `heroMail.mailType`。已保留并使用邮件类型：嘉莉缇 `mail_fav_hero_025_1`（档案 type=1、邮件 mailType=2）恢复彩色 `mail_list_new_task` 和“档案奖励”；普通邮件按任务优先、附件次之区分，全页不使用灰色图标。档案奖励从对应档案配置读取。
- 邮件列表悬停不再继承公共 ghost 按钮的深色文字；邮件奖励复用 `UiItemCard` 紧凑图标模式，边框、背景与图标比例和物品图鉴一致。银币/氪金从物品表读取品质 5，修正此前写死品质 1；数量保留 1000/100。

- 去掉 `/runes` 的 1 条重复声明、`/partner-mails` 的 3 条重复声明，各保留一次注册及原有懒加载页面。发现 `images/eggs` 已存在而魔物收益/招募仍引用不存在的 `images/pet/eggs`，同步修正页面、生成器并重建 `parsed/gacha.json`。
- 原先固定手腕角度与肘部弯曲分支组合会产生反折，迦南较长前臂还会把肘点推到肩上方。掌心偏移现纳入有效前臂长度，重新适配肩点、上下臂比例及握点；袖筒用曲线衔接。
- 自由换姿插值肩角和相对肘角；改变弯曲方向时经过展开姿势，避免第一帧切换 IK 分支或经过反向折叠。拿竿和放竿仍按同一道具轨迹保持掌心接触。
- 加载失败测试改为仅拦截 SVG 的 fetch 下载；此前通配路由误拦截 Vite SVG 模块，导致角色尚未挂载就失败。保留角色、存储选择及重试成功的断言不变。

## 重构与维护

- 项目废弃与冗余文档治理：
  - 清理 `E:\Desktop\html\myrzg` 根目录无关的 Koishi 机器人 README.md；清理 `vue-myrzg` 根目录残留的 `test-monsters.js`、`clean_dist.bat` 与临时截图 `gacha-cost-fixed.png`。
  - 将 8 月底旧副本工程 `DeepSeekflash` 与 `DeepSeekpro` 完整归档移至 `backups/legacy-workspaces/`。
  - 将已下线招募模拟功能的 `GACHA_ANIMATION.md` 与 `GACHA_DATA.md` 归档至 `vue-myrzg备份-资源/gacha-page-delete-2026-09-11/docs/features/gacha/`。
  - 将 `docs/audits/` 下 6 份阶段性审计/交接备忘归档至 `backups/audits-archive/`；将 `PROTAGONIST_PORTRAIT.md` 要点并入 `SPEC.md` 后归档至 `backups/legacy-docs/`。
  - 修剪 `SPEC.md`、`ARCHITECTURE.md`、`UI_COMPONENT_LIBRARY.md` 与 `PARTNER_MAIL_SKIN.md` 中已被下线的 Gacha 路由/演出规则以及重复粘贴的邮件阅读器说明段落，更新 `docs/README.md` 与项目根目录 `README.md` 导航体系。
  - 响应用户要求，移除 `UI_COMPONENT_LIBRARY.md` 中关于 `#3b82f6`、`#2196f3`、`#eef4fc` 等旧蓝色系一律禁用的陈旧条款及自查清单项，解除对合法特定高亮/标签配色的过度限制。

- 按用户要求移除招募模拟页面及入口：先完整备份 `GachaView.vue`、`components/gacha/`、`useGachaAudio.js`、`useGachaSimulation.js` 至 `E:\Desktop\html\myrzg\vue-myrzg备份-资源\gacha-page-delete-2026-09-11`，再删除页面代码、路由、导航入口及 App 专属场景判断。招募数据解析仍保留，供物品来源数据管线使用。

- 新增 `mascot-drawing-details.mjs` 保存逐角色精修曲线，离线合并后导出 SVG。`mascotParts` 可选提取关节局部 `upperArm/cuff/thigh/shin/boot` 及左右专属分件，公共四肢组件将服饰与真实关节一起运动；模型配置 `satchelLayer` 决定配件前后层级，不复制配件或动作。掌心尺寸回归改为测量真实 `.mascot-palm`，避免将不对称袖口误计为手掌大小。
- 删除整张插画平移/旋转的 generic 假动作分支及其 CSS，移除后续角色的 idleOnly。`mascotRedrawnModels.js` 与三份批次配置复用 `MascotArm` / `MascotLeg`、动作调度和鱼竿轨迹，服装变体按角色启用；后续 SVG 保存语义分件，四肢由运行时模型装配。新增离线手绘曲线源 `scripts/dev/mascot-drawings*.mjs` 和真实页面对照检查 `review-mascots.mjs`，不修改参考 PNG。

- 新增 `partnerMailData`，邮箱与角色档案共用 `parseHeroMail`，图标/标签集中在 `getPartnerMailPresentation`；邮件奖励复用 `acquisitionRules.parseRewardGroups`，普通任务来信不挪用任务完成奖励。`heroParser` 输出并缓存独立 `mailboxes`，角色图鉴原隐藏规则保留；`resourceSchemas` 检查新结构。仅重建 `parsed/heroes.json`，未改动原始表。附图按原目录 `uipanel/emailpanel/heromailimg/01.png` 导入，先核对项目无同内容副本。

- `mascotModels.js` 新增 002 配置，复用现有动作、关节、道具和暂停机制。`hero-002-idle.svg` 添加身体/头部/前饰/双侧长发语义分组，装配时去除原静态手臂；`MascotArm` 增加绑带袖口变体，`MascotLeg` 增加米托拉袜靴变体。通用长发内层由模型的随身物件配置控制，有限片段清理后通过 CSS 变量保留坐姿状态，不增加动画库或常驻逐帧脚本。
- 按用户要求删除 `public/images/eggs` 中 38 张带 `#` 的图片（304,057 字节），38 张无编号版本保留。35 对原本完全相同，另 3 对（pet_008、pet_094、pet_mask）有差异；删除前全部备份并逐文件校验 SHA-256。
- 新增只读图片检查工具及来源对照报告；后续按用户要求删除 `MonstersView` 并合并邮件/招募副本，普通图鉴直接使用 `PicHandBookPanel_Atlas`，不保留旧文件名映射。晶石、尖刺预览原像素保留，分别命名为 `model-previews/obj_mon055.png`、`obj_mon069.png`；蛛网直接复用 `Common_ItemIcon/item_10043.png`。详情数据改为直接消费 `portraitPath`，幼体使用原图集 `colect_mon_013_s`。
- 邮件图集改为 `EmailPanel_Atlas`，与招募共享 `Common_Atlas` 通用素材；信纸和招募揭晓纹理分别归原 `uipanel/emailpanel`、`uipanel/herogachashowpanel`。招募导入脚本与 290 条资源清单已同步，实际重跑未重建旧副本。全量图片复查内容/像素完全重复均为 0 组，解码失败 0 个；文件 2972 → 2873，减少 1,152,634 字节。102 组同名不同版本（原图/加工图/不同裁切）继续按原用途保留。
- 将用户“取自哪个原图集/业务目录，就保留哪个目录名”的要求写入资源规范；记录 MonstersView、Shop、CommonAtlas、邮件和招募等旧目录的来源与迁移依赖，避免后续继续按页面复制资源。
- 将希尔专用装配入口整理为 `MascotScene.vue`、`MascotFigure.vue`、`mascotParts.js`、`useMascotChoreography.js`，增加 `mascotModels.js`。共用 `MascotArm` / `MascotLeg`、场景片段和道具路径；既有 `.hil-*` 内部选择器保留，适用范围改为关节能力标记。
- 原 SVG 通过 `?raw` 纳入构建与开发更新追踪。没有增加动画库或常驻逐帧 JavaScript；后台、不可见、暂停、减少动态效果及卸载取消仍复用公共机制。
- 相似人形可复用流程和关节；新增角色仍需拆件、调整关节与服装，并核对武器、握点和遮挡，不能自动套色即视为适配完成。

## 验证

- 文档与冗余清理后执行 `npm run build`，全量数据预解析及 Vite 生产构建 7.37s 顺利通过，未损坏任何现有数据管线与页面产物。

- 重新按 Unity `HeroPoolPanel` 预制体校准抽卡页：`Heros` UITexture 为 1680×1000 全屏，池标题 `periodType` 为 312×76，兑换按钮为 260×72，单抽/十连按钮均为 292×72；移除按截图高度写死的短横屏缩放，卡池前景改为原始 1680×1000 画布填充。`gacha.spec.js` 桌面/手机 10/10 通过，Vite 构建通过。

- 按原游戏 1039×463 横屏截图复核 `/gacha`：新增短横屏专用 HeroPoolPanel 锚点覆盖，校准左侧页签/卡池条、右侧标题区和底部单抽/十连宽度；已查看前后对比截图。`gacha.spec.js` 桌面/手机 10/10 通过，Vite 构建通过。

- `/gacha` 原设计复核：对照 `HeroPoolPanel_Atlas`、`heropoolpanel` 原资源及 `GachaStage.vue`，确认背景、封面、标题、页签、UP、兑换和抽卡按钮均使用原 PNG；`tests/ui/gacha.spec.js` 桌面/手机 10/10 通过，覆盖抽取、重载、跨池、魔物蛋、重置、异常数据、暗色主题和退出布局。

- 完成本轮逐角色精修后，在 4187 重新运行 `hil-actions.spec.js`、`hil-sequence.spec.js`、`sidebar-mascot.spec.js`、`mascot-catalog.spec.js` 桌面全组：387 项通过、1 项设备条件跳过（14.1 分钟）。涵盖全部 34 名的四动作、肘腕和掌心握点、完整进出场、暂停/后台、动作记忆、五批选择和 900/700 高窗口；本轮截图与结果位于项目同级 `.tools/mascot-fidelity-tests/`。
- 已查看全部五张最终原图/四动作对照图，并放大核对修正角色及多名角色的取放鱼竿、起身、坐下、托腮中间帧。`review-mascots.mjs` 最终记录 34 名、136 个静态动作画面，越界 0、SVG/浏览器错误 0，每名均装配双臂双腿；结果保留在项目同级 `.tools/mascot-fidelity-review/`。全部实测站立身高与配置误差小于 .001；首批七人 28 个动作截图对比无可见造型变化，非完全相同截图的最大通道差为 1/255。
- 最终 `node node_modules/vite/bin/vite.js build --logLevel error` 与相关改动差异空白检查通过。26 份离线导出结果与仓库 SVG 一致，005 为独立手绘源；34 份 SVG 分件合计 226,817 字节（约 221.5 KiB），无内嵌位图。该数值不是整页加载大小或真机性能测量。此次为 UI/矢量模型修改，未重建游戏数据、执行全量数据 verify、同步 Android 或发布。

- 正文箭头位置调整后，邮件皮肤/奖励详情桌面与手机专项 4/4 通过；另用真实贝尔卡附件信、米托拉无附件信在两种视口核验箭头水平居中、位于正文下方且不压奖励，滚到底后消失，已查看桌面截图。Vite 构建和差异空白检查通过，未重建数据或发布。

- 等高标题/手机布局：4187 邮件专项 8/8 通过，包含既有皮肤、全部邮件来源、奖励详情与新增标题拟合/手机切换检查。验证桌面 1440→1100→1440、手机 390→320→390 时卡片等高、标题单行且使用能容纳的最大整数字号；横向滚动和前后按钮正确更新正文及边界状态。已查看桌面和手机截图；Vite 构建与差异空白检查通过。未重建数据、运行全量横屏长信专项或发布。

- 邮件称呼与奖励跳转：单元测试 6/6 通过，遍历原邮件确认无 `callName/myName` 占位残留，并核对货币和道具真实物品 ID。既有皮肤/全部来源 UI 专项 4/4 通过；新增跳转测试初轮误用系统弹窗定位器，改用实际物品详情容器后桌面/手机 2/2 通过，确认银币点击、物品 Enter 打开、关闭后搜索/信件/正文滚动保留。仅重建角色预解析；Vite 构建、差异空白检查通过，未发布。

- 邮箱补全：邮件/公共奖励单元测试 21/21 通过；4187 邮件皮肤和完整来源专项桌面/手机 4/4 通过，覆盖普通信、剧情附件、隐藏图鉴发件人、标题搜索与正确附件数量。全量邮件图片引用检查无缺失，图标统计为档案任务 38、附件 22、普通信 1；普通任务邀请用构造数据验证，当前原表没有这种条目。
- 邮箱切至角色图鉴再返回后，缓存仍保留 36 个邮箱；已查看迦南剧情附件信截图。Vite 构建及差异空白检查通过，构建产物含全部 61 封与附图。同步 SPEC、ARCHITECTURE 和邮件专题说明；本轮未运行全量数据构建、横屏长信专项或发布。

- 邮件标题居中修正后，4187 邮件皮肤桌面/手机专项 2/2 通过，已查看桌面截图确认黑条中的标题位置。本轮仅修改标题 CSS，未重复构建或发布。

- 鱼竿初始姿势调整后，4187 专项 9/9 通过：三名角色分别覆盖秋千退出→钓鱼出现/转身/坐下/伸手期间竿柄与竿尖位置不变、拿竿首帧无跳位且手掌贴合，以及完整取放/收线和拿竿暂停恢复。已查看视频逐帧图及米托拉出现、伸手和拿竿画面（`.tools/mascot-rod-start`）；Vite 构建通过，文档差异空白检查通过。未执行数据重建、全量 verify 或发布。
- 邮件字体路径、字色与间距调整后，4187 的 `partner-mails.spec.js --grep 'game mail skin'` 桌面/手机 2/2 通过，覆盖悬停不变色、彩色任务图标和图鉴奖励框；已查看两端截图。Chrome 实际字形检查确认标题/副文字使用 `HarmonyOS_Sans_SC`、正文/发件人使用 `HarmonyOS_Sans_SC_Bold`，仅请求现有两款 SC 字体且无字体失败请求；标题至发件人间距为 8px，数量右侧内缩为 7px。Vite 构建通过；未保留新增字体、重建数据或发布。

- 米托拉钓鱼右臂修正后，4187 钓鱼/肘腕/拿竿暂停专项 12/12 通过，覆盖三名关节角色及完整钓鱼退出到思考。取放鱼竿增加首末帧和五个中间帧的实际握点检查，米托拉同时检查右肘朝外下沉、腕部对齐和可见竿线不越界。初轮新增道具范围断言误计了透明的鱼形，改为采样可见竿线后最终通过；未放宽关节与握点断言。已查看修正后取竿起始、中段、末段及矮窗口截图（`.tools/mascot-mitora-elbow-fixed`），Vite 构建和文档差异空白检查通过；未执行全量数据重建或发布。
- 邮件奖励尺寸与边距调整后，Vite 生产构建通过，4187 邮件皮肤桌面/手机专项 2/2 通过；检查两端截图，标签及两格奖励完整显示。仅修改局部 CSS，未重建数据、发布或重跑横屏长内容专项。

- 邮件类型与货币品质专项单元测试 2/2 通过，逐封对照原邮件/档案配置，并修改测试物品品质以防止再次写死框色。`npm run verify` 完整数据重建、生产构建与产物验收通过；4187 邮件皮肤桌面/手机专项通过，覆盖彩色任务图标、档案奖励、物品图鉴同款框、数量、悬停颜色、筛选及明暗主题，已查看截图。本次未改邮件横屏高度布局，未重跑原横屏长内容测试，未发布。

- 米托拉补齐后，4187 三份吉祥物桌面专项最终 42 项通过、1 项设备条件跳过；三人的姿势、肘腕、握绳、握竿、鱼线/浮漂、完整进出场、暂停/后台/减少动态效果和最后一次点选均通过，七人身高、站位及预览回归通过。新增长发坐下过程连续变化与起身恢复断言。初轮米托拉 8/10 通过，两个逐阶段检查因浏览器往返错过短片段失败；改为在检查用例内暂停新建的有限片段，再逐帧推进，真实时间播放与暂停用例保持原样，最终全组通过。已查看正常/矮窗口动作与托腮、拿竿中间帧截图（`.tools/mascot-mitora`）；Vite 构建通过，未重建游戏数据、执行全量 verify、真机测试或发布。
- 统一显示大小后，在 4187 完成 `sidebar-mascot.spec.js`、`hil-actions.spec.js`、`hil-sequence.spec.js` 桌面回归：32 项通过、1 项设备条件跳过。新增七人待机可见身高一致断言，原有落脚点、画布、两种窗口尺寸、角色预览和右侧问号检查通过；思考/秋千/钓鱼完整进出场、握点、暂停、减少动态效果与刷新记忆均通过。已检查最终并排预览及小窗口菲莉娜截图（`.tools/mascot-unified-size`），最终 Vite 构建与相关差异空白检查通过；未重建游戏数据或发布。
- 图片合并完成后 `npm run verify` 全部通过，包含完整数据重建、Vite 构建和产物验收；52 张正式怪物卡片、128 个形态引用均有实际图片。怪物图片、招募数据和演出清单单元测试 25/25 通过，模型预览哈希保持不变；源码和运行时产物未残留已删除图片目录引用。
- 浏览器专项初轮 22 项：20 通过、2 失败。三张特殊怪物图和普通图鉴、幼体图片在桌面/手机均通过，邮件原图/主题/选择及招募按钮/结算通过，已查看截图。桌面招募首轮在首页按钮加载前超时，独立端口 4176 原用例复验 1/1 通过；未修改演出机制或放宽断言。
- 手机邮件长内容的 844×390 横屏用例仍未通过：`.mail-body` 不可见导致滚动操作超时。这是本轮额外发现的布局限制，图片已正常加载；本次只变更资源引用，未修改该区域尺寸或跳过此测试。相关截图/trace 保留于 `test-results/partner-mails-mail-viewpor-f2339-olumn-scrolls-independently-mobile`。专项测试服务 4174、4176 已停止，未影响其他预览服务。
- 七人统一站位后，4187 `sidebar-mascot.spec.js --project=desktop` 最终 12 项通过、1 项设备条件跳过；新增场景逐个验证七人在 900px/700px 高窗口的待机/思考画布、控制行、缩放和落脚点一致，问号统一右侧，并覆盖原有选择、失败重试、暂停与减少动态效果。已查看迦南、露帕、菲莉娜及七人选择窗截图（`.tools/mascot-unified-stage`），最终 Vite 构建通过。
- 菲莉娜初轮居中检查发现尾羽旋转的宽松包围框越界；将旋转固化到矢量路径后角色思考/七人切换复验 2/2 通过，再纳入上面的全组回归。期间一次构建遇到其他并行修改的旧导入缺失，未改动该模块，最终重新构建通过。本轮未重新生成游戏数据或发布。
- 菲莉娜问号定位/尾巴修正后，4187 关节角色思考及暂停回归通过；已检查菲莉娜正常和 700px 高窗口截图（`.tools/mascot-bubble-049`），最终 Vite 构建通过。
- 路由/蛋图片处理：`node --test tests/unit/gacha.test.mjs tests/unit/pet-eggs.test.mjs` 21/21 通过，含卡池图片引用检查；`node scripts/parse/gacha.mjs` 生成 4 个卡池；`node node_modules/vite/bin/vite.js build --logLevel error` 通过。图片全量只读扫描解码错误为 0；本轮未执行全量数据构建、全站 UI 回归、真机验证或发布。
- 按用户要求再次合并招募目录中的 33 张原立绘、33 张碎片和 32 张原蛋图：分别共享 `chara/l`、`HeroInfoPanel_Atlas`、`eggs`，并删除 `gacha/portraits`、`gacha/fragments`、`gacha/eggs`。导入脚本、演示数据和角色/物品引用均已同步；244 个受影响文件先备份到 `E:\Desktop\html\myrzg\vue-myrzg备份-资源\gacha-sharing-2026-09-11`，未重编码图片，节省约 9.2 MiB。
- 路由静态复核：19 个 path、18 个 name 无重复；`public/images/eggs` 和构建后的 `dist/images/eggs` 均为 38 张无编号文件。修改范围差异空白检查通过。
- 问号间距微调后，4187 思考及尺寸专项 4/4 通过；已查看正常窗口与 700px 高窗口的可见问号截图（`.tools/mascot-bubble-close`），最终 Vite 构建通过。
- 恢复斜托下巴后，在 4187 完成姿势/肘腕/尺寸专项 6/6；新增斜托位置与过渡袖口连接检查后的思考、钓鱼退出到思考、暂停与最后一次点选专项 6/6 通过。已查看两人最终托腮截图（`.tools/mascot-think-natural`）；最终 Vite 构建通过，未做数据重建或发布。
- 使用现有 4187 开发服务。手部修正阶段运行 `hil-actions.spec.js`、`hil-sequence.spec.js`、`sidebar-mascot.spec.js` 桌面专项：29 项通过、2 项新增肘部检查失败、1 项设备条件跳过；调整握绳高度与握竿位置后，前两份专项复验 20/20 通过。
- 进一步调整换姿肘角插值并增加中间帧检查后，`hil-sequence.spec.js --project=desktop` 最终 10/10 通过。覆盖完整进出场、实际握点与鱼线/鱼漂连接、双肘下沉、腕部角度、模型/手掌缩放、暂停、后台冻结、最后一次点选、减少动态效果及卸载取消。
- 加载失败夹具修正后的相关专项 5/5 通过，包含两人的思考/尺寸检查与失败重试；后续整组运行中角色切换及原有选择窗测试也通过。没有把首次失败记作首轮全通过。
- 已查看最终两人的思考、迦南秋千/钓鱼、拿竿以及抬手中间帧截图。截图目录：`E:\Desktop\html\myrzg\.tools\mascot-preview`。
- 最终 `node node_modules/vite/bin/vite.js build --logLevel error` 通过；权威文档差异空白检查通过。本次为 UI/矢量动作修改，未重建游戏数据、运行全量 verify、进行 Android 真机性能测试或发布。

## 发布与回退

- 本轮图片合并/改名处理的 485 个源文件均在删除前校验并备份至 `E:\Desktop\html\myrzg\backups\image-consolidation-2026-09-11`，包含逐文件路径与哈希清单。可依原路径恢复；恢复旧结构需同步恢复代码引用。原游戏资源包和 Git 暂存区未改动，未发布线上版本。
- 38 张删除图片的完整备份位于 `E:\Desktop\html\myrzg\backups\image-cleanup-2026-09-11\eggs-hash`，按原名复制回 `public/images/eggs` 即可恢复。路由与蛋图片路径仅本地修正，未修改 Git 暂存区，未发布。
- 仅本地实现与构建验证，预览端口 4187。未发布线上版本、同步 Android 或生成热更包；参考 PNG 与游戏原表未修改。

## 未完成事项

- 手机邮件长内容横屏布局测试未通过，尚需单独处理；本轮图片去重与特殊图片识别已完成，未将浏览器专项报告为全通过。
- 原 96 组完全重复已全部处理。102 组同名不同版本包含有意保留的招募原图与图鉴加工图，不按同名删除；其他无关目录的批量改名不在本轮范围。
- 吉祥物后续适配已覆盖全部 34 名，062、053、034、049 的关节场景也已完成。

## 文档同步

- 同步更新 `SPEC.md`、`ARCHITECTURE.md`、`UI_COMPONENT_LIBRARY.md`、`docs/README.md` 与 `vue-myrzg/README.md`，清除失效链接与废弃路由条目。

- 本轮服饰精修的局部分件、佩饰层级与成品检查要求同步到 `SPEC.md`、`ARCHITECTURE.md`、`UI_COMPONENT_LIBRARY.md`；新增逐角色形象复核表并接入 `docs/README.md`。
- 邮件 `mailType` 与档案 `type` 的区别、原源码判断顺序、货币品质来源及共享卡片参数同步到 `SPEC.md`、`ARCHITECTURE.md`、`UI_COMPONENT_LIBRARY.md`、`features/PARTNER_MAIL_SKIN.md`。

- 米托拉适配已同步 `SPEC.md`、`ARCHITECTURE.md`、`UI_COMPONENT_LIBRARY.md`，明确三名关节角色、四名原分组角色，以及长发、袖口和袜靴变体约定。
- 图片整理要求同步到 `SPEC.md`、`ARCHITECTURE.md`；新增 `audits/IMAGE_RESOURCE_AUDIT_2026-09-11.md` 并补入文档导航。
- 已同步 `SPEC.md`、`ARCHITECTURE.md`、`UI_COMPONENT_LIBRARY.md` 中的公共装配、角色适配、手臂约束与进出场规则。
- 吉祥物现状已同步到 `SPEC.md`、`ARCHITECTURE.md`、`UI_COMPONENT_LIBRARY.md`：全部 34 名支持四动作，后续 27 名采用首批风格的手绘分件，选择窗按每批 7 人分页。明确模型源路径随懒加载侧栏模块进入、独立请求和预览按当前角色/批次管理，避免误称所有源路径逐角色加载。
- 目录初次扩展时的批次测试 1/1、待机目录测试 1/1 与 Vite 构建曾通过；该轮 27 名仅待机的实现现已由完整手绘关节版本替换，不能将旧结果当作本次四动作验收。
